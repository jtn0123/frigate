# Vulnerability scanner for the fork's own dependency locks (E7). Compile with:
#   uv pip compile fork/requirements-audit.in --universal --python-version 3.11 \
#     --generate-hashes --only-binary :all: -o fork/requirements-audit.txt
pip-audit==2.10.1
