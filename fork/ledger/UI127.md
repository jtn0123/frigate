| UI127 | The phone alarm carries its count and stops looking like a seventh tab | hunks in `web/src/components/navigation/Bottombar.tsx` | The bottom bar gave a status the same treatment as the six destinations beside it: a bare warning triangle in a tab slot, with no count and nothing to say it opens a sheet rather than navigating. It now sits behind a divider, carries the issue count as a badge the way the desktop chip does, announces `aria-haspopup="dialog"`, and its label names how many issues there are. The drawer it opens is unchanged. | fork-only |
