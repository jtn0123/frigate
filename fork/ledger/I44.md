| I44 | Auto-file drafts that have earned it | `frigate/fork/classification_prefetch.py` (`auto_file`), `frigate/fork/classification_suggestions.py` (`sure_draft`, `kept_rate`, `train_files_for_event`, `auto_filed` in `summarize_provenance`), `frigate/config/fork/classification_suggestions.py` (`auto_file`), `frigate/api/defs/response/fork_classification_suggestions_response.py`; `web/src/components/fork/classification/SuggestionStatusBar.tsx`; tests; `fork/CLASSIFICATION-SUGGESTIONS.md` | Every draft still needed a click, even after the report showed a class was kept 100% of the time. With `classification.suggestions.auto_file.enabled` (default false) the I43 worker files an event's train images itself when the local text match and Jev name the same class and people have kept that class's drafts at least `min_kept_rate` (0.9) of the time over at least `min_drafts` (20) reviews. Auto-filed images are recorded with `auto: true`, shown as their own count in the report and status line, and never feed the rate that unlocks them, so trust can only be earned from a person's confirmations | n/a (fork-only) |
