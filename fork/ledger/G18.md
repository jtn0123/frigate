| G18 | backend: recordings/events query performance from upstream 0.19 | hunks in `frigate/api/media.py` (`vod_ts`, `recording_clip`) and `frigate/api/record.py` (`recordings`, `delete_recordings`); range cases in `frigate/test/http_api/test_http_media.py` | Range lookups on `recordings` used `start_time BETWEEN ... OR end_time BETWEEN ... OR (...)`, which SQLite cannot answer from the `(camera, start_time)` index, and `/recordings/{camera}` had no lower bound on `start_time`. They now use `camera = ?`, `start_time >= start - MAX_SEGMENT_DURATION`, `start_time <= end` and `end_time >= start`: the same rows, since a segment is never longer than `MAX_SEGMENT_DURATION`, found by an index range seek. Upstream changed `vod_ts` and `recordings`; `recording_clip` and `delete_recordings` had the same clause and take the same bounds here | backport of upstream #23862 |
