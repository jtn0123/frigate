| G18 | backend: recordings/events query performance from upstream 0.19 | hunks in `frigate/api/media.py` (`vod_ts`, `recording_clip`), `frigate/api/record.py` (`recordings`, `delete_recordings`, `all_recordings_summary`, `no_recordings`), `frigate/record/cleanup.py` (`expire_existing_camera_recordings`, the deleted cameras in `expire_recordings`) and `frigate/storage.py` (`calculate_camera_bandwidth`, new `_recent_bandwidth`); `frigate/test/test_record_cleanup_deleted_cameras.py` (new); cases in `frigate/test/http_api/test_http_media.py` and `frigate/test/test_storage.py` | Recordings queries that fell back to full scans or large sorts on big databases are reshaped into bounded index seeks, with the same results. Range lookups used `start_time BETWEEN ... OR end_time BETWEEN ... OR (...)`, which SQLite cannot answer from the `(camera, start_time)` index; they now use `camera = ?`, `start_time >= start - MAX_SEGMENT_DURATION`, `start_time <= end` and `end_time >= start` (the same rows, since no segment is longer than `MAX_SEGMENT_DURATION`). Upstream changed `vod_ts` and `recordings`; `recording_clip` and `delete_recordings` had the same clause and take the same bounds here. `/recordings/summary` walks the days with `EXISTS` probes and skips empty gaps with per-camera `MIN` seeks instead of bucketing every row (it stays a plain `def`, G3); `/recordings/unavailable` queries each camera with a bounded `start_time` and merges in Python; the expiry query gets a `start_time <` bound; deleted cameras are listed with one index seek each instead of `camera NOT IN (...)`; bandwidth filters zero-size segments in a `CASE` projection (a `segment_size` predicate picked the `(camera, segment_size)` index plus a full sort), falls back to the last 1000 rows when the last 100 are all zero-size, and `needs_refresh` counts at most 50 rows. Upstream's sub stream, keyframe and player parts of #24011 are not taken | backport of upstream #23862, #24011 |
