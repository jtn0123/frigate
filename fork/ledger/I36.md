| I36 | CI | `.github/workflows/fork-checks.yml` (`concurrency`) | `cancel-in-progress: true` applied to pushes as well as pull requests, so a merge into `next` cancelled the run of the merge before it (ce9770b62 was cancelled 11 minutes in when 66774e9b6 landed). Intermediate merge commits never got a finished check or their own Sonar branch analysis, so a regression could not be pinned to one merge. Only pull request runs are superseded now; branch runs queue and all finish. Follow-up the same day: queuing in one group per branch still lost runs, because a group holds a single pending run (c4bb2e7f7 and 0004f0494 were replaced while 4730d9df1 ran), so a push now uses its commit SHA as the group and the runs go in parallel | n/a (fork-only) |
