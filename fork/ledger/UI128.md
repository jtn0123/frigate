| UI128 | User-facing strings in labels, placeholders and alt text go through i18n | hunks in `web/src/components/ui/sidebar.tsx`, `web/src/components/indicators/activity-indicator.tsx`, `web/src/components/menu/GeneralSettings.tsx`, `web/src/components/ws/WsMessageRow.tsx`, `web/src/components/classification/wizard/Step2StateArea.tsx`, `web/src/views/recording/RecordingView.tsx`, `web/src/pages/Settings.tsx`, `web/src/components/overlay/detail/ObjectPathPlotter.tsx`, `web/src/components/overlay/CreateUserDialog.tsx`, `web/src/components/settings/ObjectMaskEditPane.tsx`, `web/src/components/input/ImageEntry.tsx`, `web/src/components/settings/wizard/Step2ProbeOrSnapshot.tsx`, `web/public/locales/en/fork.json`, and the two tests that asserted the English literal | A grep for `aria-label="[A-Z]`, `placeholder="[A-Z]` and hard-coded `alt` turned up thirteen user-facing strings shipped as English literals, in a repository whose own guide forbids it. Screen-reader users in every other locale heard English for the sidebar toggle, every loading spinner, the JSON copy button, the recording view's two stream controls, four select placeholders and three image descriptions. They now read from `a11yLabels` in the fork namespace. The sidebar trigger also carried the same string twice, as `aria-label` and `title`, so the tooltip and the accessible name duplicated each other; the `title` is gone. | fork-only |
