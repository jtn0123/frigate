| I49 | Unsure images first | `frigate/fork/classification_suggestions.py` (`train_file_verdict`, `sure_train_files`), `frigate/fork/classification_prefetch.py` (`worth_filing`), `frigate/config/fork/classification_suggestions.py` (`auto_file.max_model_score`); `web/src/lib/fork/classification-suggestions.ts` (`groupScore`, `orderGroups`), `web/src/hooks/fork/use-train-order.ts`, `web/src/components/fork/classification/SuggestionStatusBar.tsx` (toggle), hunk in `web/src/views/classification/ModelTrainingView.tsx`; tests, e2e case | Upstream's docs say to favor images the model scored under 90% and avoid bulk-training the ones it already nails. The score is in every train file name. Auto-filing now leaves images the model scored at or above `max_model_score` (0.9) as the drafted class in the grid, and the grid gets an "Unsure first" switch (remembered per browser) that lists the least sure events first instead of newest first | n/a (fork-only) |
