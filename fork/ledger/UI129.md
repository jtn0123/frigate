| UI129 | The camera filter says what its switches do and its groups look like controls | hunks in `web/src/components/filter/CamerasFilterButton.tsx`, `web/public/locales/en/fork.json` | Two problems in one popover. The camera group entries were `button` elements styled as plain text, sitting between two rows of switches, so a group read as a heading rather than something to press; they now carry the camera count and a chevron and stand a row tall. And with "All Cameras" on, every per-camera switch still drew as off, which reads as "nothing is selected" when the opposite is true; a line under the separator now says all cameras are included and what turning one on does. Neither change touches the filter's behaviour. | fork-only |
