| C30 | web: share | `web/src/components/fork/ShareClipButton.tsx` (+ cases in `.test.tsx`), `web/src/components/fork/ActiveShareLinks.tsx` (`revoke`) (+ cases in `.test.tsx`), keys `clipShare.{create,limitReached}` in `web/public/locales/en/fork.json`, cases in `web/e2e/specs/fork/clip-sharing.spec.ts` | Opening the share dialog (UI11) always POSTed a new public link, and the server does not reuse one per event. At the cap of 50 links (E16) the POST answers 429, the catch closed the dialog, and the list with the Revoke buttons only exists inside that dialog, so nothing could be revoked until a link expired (up to 168 hours). The dialog now opens on the active list and makes a link only from a "Create link" button (disabled while a request runs). A create failure keeps the dialog and the list open and shows an inline alert: "You have reached the limit of active share links. Revoke a link below, then try again." for a 429, the general message otherwise; the button retries, and revoking a link clears the limit message. Every create and every close bumps a request id, so a response that lands after close and reopen is dropped instead of swapping the QR code (the list is still refreshed, because that link exists). After the dialog's own link is revoked the button is offered again. A failed revoke now revalidates the list, and a 404 (the link expired or was revoked elsewhere) counts as gone: no error toast, the row is dropped and the dialog is told | fork-only |
