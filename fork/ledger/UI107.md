| UI107 | UI: System metric cards keep their height without samples | new `web/src/components/fork/MetricEmptyState.tsx` (+ test), `web/src/hooks/fork/use-live-stats-history.ts` (+ test), `web/e2e/specs/fork/state-polish.spec.ts`; `web/public/locales/en/fork.json`; hunks in `web/src/views/system/GeneralMetrics.tsx`, `web/src/views/system/CameraMetrics.tsx`, `web/src/views/system/EnrichmentMetrics.tsx`, `web/src/components/graph/SystemGraph.tsx` | Detector inference, CPU and memory and Process CPU and memory cards were 60 px title strips when their series had no sample (a stats snapshot without processes, or a detector that reports no CPU or memory), and every bar graph was zero height until ApexCharts mounted, so the page jumped as data arrived. A card with no sample now shows "Waiting for the first stats sample" at the height of one graph, a series without samples no longer draws a row of zero bars, and each bar graph reserves its 136 px before the chart mounts. Cameras and Storage use other graph components that already hold a skeleton or always have data. The General, Cameras and Enrichments tabs also ignored live stats while `stats/history` was empty, so right after a restart they stayed on skeletons; they now share `useLiveStatsHistory`, which grows an empty history from live samples and caps it at the backend's 80. | fork-only |
