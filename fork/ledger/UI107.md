| UI107 | UI: System metric cards keep their height without samples | new `web/src/components/fork/MetricEmptyState.tsx` (+ test), `web/e2e/specs/fork/state-polish.spec.ts`; `web/public/locales/en/fork.json`; hunks in `web/src/views/system/GeneralMetrics.tsx`, `web/src/components/graph/SystemGraph.tsx` | Detector inference, CPU and memory and Process CPU and memory cards were 60 px title strips when their series had no sample (a stats snapshot without processes, or a detector that reports no CPU or memory), and every bar graph was zero height until ApexCharts mounted, so the page jumped as data arrived. A card with no sample now shows "Waiting for the first stats sample" at the height of one graph, a series without samples no longer draws a row of zero bars, and each bar graph reserves its 136 px before the chart mounts. Cameras and Storage use other graph components that already hold a skeleton or always have data. | fork-only |
