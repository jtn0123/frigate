| UI101 | UI: legibility and layout fixes from the second UI walk | new `web/e2e/specs/fork/ui-legibility.spec.ts`; `web/themes/fork-appearance.css`, `web/public/locales/en/fork.json`; hunks in `web/src/components/ui/sonner.tsx`, `web/src/components/Statusbar.tsx`, `web/src/views/events/EventView.tsx`, `web/src/views/settings/RegionGridSettingsView.tsx`, `web/src/components/config-form/sections/BaseSection.tsx` | A second walk (dark theme, 1920 and 1280 px desktop, all 59 Settings sections, 11 overlays) found six problems. Phone toasts, moved to the top in UI100, covered a pushed page's back button and camera switch: they now start 64 px down. `text-warning` used the `--warning` background token, a dark brown in both modes, as a text color in 19 fork call sites (1.3:1 on dark): dark mode of the default and high-contrast themes now reads `--warning-foreground`. The status bar capped messages at half the bar and cut a long one mid-word at 1280 px: the stats keep their width, messages take the rest and carry a `title`, and the bar's dark text moves from 3.4:1 to `primary-variant`. The phone Alerts count was red-800 on the selected tab's dark gray (1.5:1): red-400 in dark. The region grid showed the browser's broken-image icon when there was no image: it shows a message. Save sat at the far edge of a 1920 px window, 900 px from the fields: its bar is `max-w-5xl` like the form. | fork-only |
