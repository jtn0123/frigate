| UI103 | Settings: Users page error state | new `web/src/components/fork/settings/UsersLoadError.tsx` (+ test); `web/src/components/fork/ErrorState.tsx`, `web/public/locales/en/fork.json`; hunk in `web/src/views/settings/AuthenticationView.tsx` | Users and Roles waited on `/api/users` with a spinner that never ended when the request failed. They now show the shared ErrorState: a 401 or 403 says only admins can view and manage users, anything else shows the generic sentence plus the server message, and Retry calls SWR `mutate`. ErrorState gains an optional `message` to replace its generic sentence. The upstream global SWR toast still fires as well. | fork-only |
