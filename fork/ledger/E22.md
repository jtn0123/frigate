| E22 | security: share links | hunks in `frigate/api/fork_share.py` (`_SlotStream` takes the playlist path and unlinks it in `release`) and `frigate/api/media.py` (`recording_clip` sets `playlist_path` on its response); cases in `frigate/test/http_api/test_http_fork_share.py` | `recording_clip` writes a `playlist_*.txt` concat list into the cache directory before it streams, and only the body generator's `finally` unlinks it. A client that left before the first chunk was read never ran the generator, so the file stayed, and nothing sweeps `playlist_*`. The pattern is upstream's, but through a share link (UI11) anyone holding a valid token could repeat it without a login against a tmpfs. `_SlotStream`, which already gives the clip slot back in exactly that case (E15), now also removes the list: when the body ends, fails, is cancelled, or is dropped unread. By then ffmpeg has either read the list or will never start, and the generator's own unlink tolerates a missing file. A list that cannot be removed is logged as a warning. The response carries the path as an attribute, a 4 line hunk in the upstream file; feeding the list on stdin as `preview_gif` does was not used, because the clip is streamed from a `Popen` rather than run to completion, so it would have changed `_run_clip_download` and its tests. The authenticated `clip.mp4` route keeps upstream's behavior. Found by a code grade | fork-only |
