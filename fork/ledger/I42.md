| I42 | Suggestion acceptance report | `frigate/fork/classification_suggestions.py` (`summarize_provenance`), `frigate/api/fork_classification_suggestions.py` (`/classification/{name}/suggestions/report`), `frigate/api/defs/response/fork_classification_suggestions_response.py`; `web/src/components/fork/classification/SuggestionStatusBar.tsx`, `web/src/hooks/fork/use-suggestion-report.ts`, hunk in `web/src/views/classification/ModelTrainingView.tsx` (`ObjectTrainGrid`); `fork/CLASSIFICATION-SUGGESTIONS.md` | Confirmations made through I41 were written to `.fork_provenance.jsonl` but nothing read them back, so there was no way to tell whether the drafts were worth trusting. `GET /classification/{name}/suggestions/report` (admin) counts, overall and per source, suggested class and camera, how many drafts were filed unchanged and what the corrected ones were changed to, so a class that keeps being mistaken for another is visible before it is trained in. Groups filed by Accept all are recorded with `bulk: true` and reported as `bulk_accepted` (overall and per class) instead of in the rate, and undone confirmations are counted as `undone` and left out of every count. The first row of the train grid counts photos with a guess against photos on the page and links to the Stats page, where Jev's budget, the kept rate and the model check now live. Its "Accept all N guesses" button opens a dialog with the guesses per class and a "Skip images too small to train well" box (checked by default), then files one event at a time with `bulk: true`, shows "Accepting 3 of 20" on the button, disables the single checks while it runs, counts a 404 "already accepted" as done, reports failures, and stops without a toast when the page is left. A single accept keeps its check disabled until the grid has refreshed, refreshes on failure too, and offers Undo in its toast (`web/src/hooks/fork/use-undo-suggestion.ts`). On phones the row is one line with a More menu | n/a (fork-only) |
