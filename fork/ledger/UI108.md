| UI108 | UI: legible in-progress export card | new `web/src/components/fork/ExportProgressCard.tsx` (+ test), `web/e2e/specs/fork/state-polish.spec.ts`; `web/public/locales/en/fork.json`; hunk in `web/src/components/card/ExportCard.tsx` | An export still being written kept the finished card's white title, drawn over a gray skeleton that never cleared because the thumbnail is not loaded until the export finishes, so "Garage - In Progress" was near invisible. In-progress exports and active export jobs now share one card in theme colors with an "Exporting" pill, the title, and a progress bar: the job's percent with its step ("Encoding · 42%") when the job reports one, an indeterminate bar otherwise. The finished card is unchanged. | fork-only |
