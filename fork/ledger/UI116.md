| UI116 | Third UI walk: Review, Explore, Faces, Logs, camera charts, Enrichments | new `web/src/components/fork/review/MarkReviewedBar.tsx`, `web/src/components/fork/explore/ExploreRowViewAll.tsx`, `web/src/components/fork/EnrichmentsEmptyState.tsx`, `web/e2e/specs/fork/pages-polish.spec.ts`; `web/public/locales/en/fork.json`; hunks in `web/src/views/events/EventView.tsx`, `web/src/views/explore/ExploreView.tsx`, `web/src/pages/FaceLibrary.tsx`, `web/src/components/icons/AddFaceIcon.tsx`, `web/src/pages/Logs.tsx`, `web/src/components/graph/LineGraph.tsx`, `web/src/views/system/EnrichmentMetrics.tsx` | Six page-level fixes found in a walk over the pages outside Settings. Review's mark-as-reviewed button sat centered in the grid with no count; it is now a bar under the last row that says how many items are unreviewed and carries the count in its label. Explore rows ended in a bare circular arrow whose only label was a tooltip, unreachable on touch; it is a labeled button with the group's count. An empty face library offered Add Face twice and the plus badge on the icon floated off the glyph; the header button is dropped while the library is empty and the badge is anchored to the corner. Logs kept Copy and Download live and the column header row up while the logs had failed to load; both now stand down. Every camera chart printed "Invalid time" on axis ticks past the end of the sample window, because `Number.isNaN(undefined)` is false and undefined reached the formatter; the guard now catches null and undefined. System > Enrichments drew a heading over an empty grid when stats carried no embeddings section; it says why and links to the settings. No behavior change beyond these. | fork-only |
