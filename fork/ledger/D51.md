| D51 | audio trial: model cache | `fork/audio_trial/model_cache.py`, `fork/audio_trial/test_model_cache.py` | `test_pinned_snapshot_detects_corruption_even_with_cached_validation` failed once in CI (#67): the cached validation is keyed on size, mtime, ctime and inode, and the test's same-size rewrite can keep all four on a filesystem with coarse timestamps, so hashing was skipped. The same window let a fast same-size swap of a model file go unnoticed. The cache now records when it verified and is only trusted once that is 2 s newer than every file it covers (git's racy-timestamp rule); until then the hashes are checked again. The test forces its timestamp change, and two new tests cover the same-tick rewrite and a cache file without a verification time | n/a (fork-only) |
