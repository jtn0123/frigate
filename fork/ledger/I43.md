| I43 | Draft a class as each description arrives | `frigate/fork/classification_prefetch.py`, hunk in `frigate/comms/dispatcher.py` (`handle_update_event_description`), `frigate/config/fork/classification_suggestions.py` (`jev.background`), shared helpers moved into `frigate/fork/classification_suggestions.py`; `frigate/test/test_fork_classification_prefetch.py`; `fork/CLASSIFICATION-SUGGESTIONS.md` | I41 only asked Jev while the train grid was open, so the first time the page opened a week of train images could be waiting on one day's budget, and every fresh page took one round trip per card. With `classification.suggestions.jev.background` (default true, only active when Jev is enabled and a key is set) the main process queues each description the moment it is saved and a daemon thread asks Jev once per custom model that classifies that label, storing the answer in the same cache the grid reads. The grid then finds its answers already there, the daily limit is spent evenly over the day, and the same worker is where I44 files the sure ones. Still only the text is sent | n/a (fork-only) |
