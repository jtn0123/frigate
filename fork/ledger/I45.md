| I45 | Check the trained model against the description | `frigate/fork/classification_prefetch.py` (`check_model`), `frigate/fork/classification_suggestions.py` (`model_verdict`, `record_model_check`, `read_model_checks`, `summarize_model_checks`), `frigate/api/fork_classification_suggestions.py` (`model_check` in the report), `frigate/api/defs/response/fork_classification_suggestions_response.py`; `web/src/components/fork/classification/SuggestionStatusBar.tsx`; tests; `fork/CLASSIFICATION-SUGGESTIONS.md` (also the "Better descriptions" prompt note) | Once a model is trained nothing watched whether it kept agreeing with what the descriptions said, so a model that started calling sedans SUVs went unnoticed until someone browsed Explore. The I43 worker now compares the model's own verdict for each described event (the sub label, or the attribute it wrote) with the draft and appends one line to `.fork_model_checks.jsonl`. The report gains `model_check` with the agreement rate, per class what the description said instead, and the latest disagreements, and the status line shows the rate with the per-class breakdown on hover. Only the two class names and ids are stored, never the text | n/a (fork-only) |
