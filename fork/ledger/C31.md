| C31 | web: share | `web/src/pages/fork/ShareClipPage.tsx` (+ cases in `.test.tsx`), `web/src/lib/fork/share-path.ts` (`isPublicSharePath`, `shareCameraName`, `basePath`) (+ cases in `.test.ts` and `qr.test.ts`), hunk in `web/src/components/fork/ActiveShareLinks.tsx` (uses `shareCameraName`), keys `clipShare.{loadFailed,playFailed}` in `web/public/locales/en/fork.json`, cases in `web/e2e/specs/fork/clip-sharing.spec.ts` | The public share page (UI11) showed "This share link was not found." for every failure but a 410, so a network error, a 500 and a 429 looked final and had no retry. Only a 404 (and a malformed token) is "not found" now; anything else is an `error` state with a Retry button that reads the link again. The `<video>` had no `onError`, so a link that expired while the page was open, or a rate-limited clip, failed silently: it now gives way to a message with the same Retry, which turns into the expired state when the link is gone. The status paragraphs have `role="status"`, the two failures `role="alert"`. The camera name showed raw underscores while the active links list cleaned them; both call `shareCameraName`. `isPublicSharePath` matched `/share/x` anywhere in a path, so any later route containing it would render the public shell and skip the login redirect. It now strips `window.baseUrl` (it is called with `window.location.pathname` as well as the router's pathname) and matches only `/share/<token>`, so `/foo/share/abcdefgh` and `/share/abcdefgh/extra` do not match | fork-only |
