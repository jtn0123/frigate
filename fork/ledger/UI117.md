| UI117 | Settings walk: roles error, override badges, Birdseye notice, reindex | new `web/src/components/fork/settings/SectionOverrideBadges.tsx`, `web/e2e/specs/fork/settings-walk.spec.ts`; `web/public/locales/en/fork.json`, `web/public/locales/en/views/settings.json`; hunks in `web/src/components/fork/settings/UsersLoadError.tsx`, `web/src/views/settings/AuthenticationView.tsx`, `web/src/views/settings/SingleSectionPage.tsx`, `web/src/components/config-form/section-configs/types.ts`, `web/src/components/config-form/section-configs/birdseye.ts`, `web/src/hooks/use-config-messages.ts`, `web/src/components/config-form/ConfigMessageBanner.tsx`, `web/src/components/config-form/sectionExtras/SemanticSearchReindex.tsx` | Four fixes from a walk over Settings. The Roles page reads `/api/users` like the Users page and so said "Could not load users" when that read failed; it names roles now, in the title and in the 403 sentence. The override badges were two copies of the same markup, one pinned to the far right of the title on desktop and one below it on phones; they are one row under the title. The Birdseye "objects mode with detect disabled" notice was styled as info although the camera silently never appears; it is a warning, says so in one sentence, and links to that camera's detect settings. A conditional message can now carry an action, resolved against the same context its condition reads. Reindex sat directly under the Enabled toggle and read as part of it; it has its own card with a heading, the warning text above the button, and the confirm dialog it already had. No behavior change beyond these. | fork-only |
