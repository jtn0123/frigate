| F4 | web dependencies | `web/package.json`, `web/package-lock.json` | Refresh compatible form, canvas, router and build dependencies; upgrade Vitest and its coverage provider together to patched 4.1.11 while retaining Vite 6 and TypeScript 5. This completes the Vitest security portion of F4; the other major migrations remain open | fork-only |
