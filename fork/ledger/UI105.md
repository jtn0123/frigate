| UI105 | Settings: Generative AI empty state | new `web/src/lib/fork/config-empty-state.ts` (+ test), `web/src/components/fork/settings/ConfigEmptyState.tsx`; `web/public/locales/en/fork.json`; hunks in `web/src/components/config-form/theme/templates/ObjectFieldTemplate.tsx`, `web/src/components/config-form/sections/BaseSection.tsx`, `web/src/components/config-form/section-configs/genai.ts` | With no providers the page showed only a small "+ Add" button. A map section can now opt in with the root uiSchema option `forkEmptyState`; Generative AI does, and shows an icon, "No providers yet", the supported providers (checked against `GenAIProviderEnum`: OpenAI, Azure OpenAI, Gemini, Ollama, llama.cpp) and an "Add provider" button that calls the same `onAddProperty`. The Save / Reset bar is hidden only while the global map is empty, saved that way and unedited (Reset would do nothing and Save has nothing to save); it comes back as soon as a provider is added. | fork-only |
