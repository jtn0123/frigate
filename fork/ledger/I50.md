| I50 | Skip tiny crops | `frigate/fork/classification_suggestions.py` (`image_size`, `too_small_train_files`, `train_files_by_event`), `frigate/api/fork_classification_suggestions.py` (`too_small` in both draft responses), `frigate/fork/classification_prefetch.py`; `web/src/lib/fork/classification-suggestions.ts` (`usableFiles`), `web/src/components/fork/classification/{SuggestionBadge,SuggestionStatusBar,ExploreSuggestion}.tsx`; tests, e2e case | Upstream's docs: crops under about 80 to 100 px get stretched three to seven times and hurt more than they help. Train images under 100 px on a side are never auto-filed. The draft still shows for people: the badge, the Explore row and the status line flag events whose every image is that small, and filing them stays the person's call | n/a (fork-only) |
