| UI110 | UI: status bar warnings collapse to a chip | new `web/src/components/fork/StatusIssuesChip.tsx` (+ test), `web/e2e/helpers/status-issues.ts`, `web/e2e/specs/fork/state-polish.spec.ts`; `web/public/locales/en/fork.json`; hunk in `web/src/components/Statusbar.tsx`; `web/e2e/specs/live.spec.ts`, `web/e2e/specs/fork/{camera-health,ui-legibility}.spec.ts` open the chip | The desktop status bar printed each warning as a full sentence, so "Host collector is missing or stale..." filled the bar and more warnings scrolled sideways. The bar now shows one chip ("2 issues", amber, red when any message is an error) that opens a popover listing each message in full with a "View details" link to the page it names (System > AI models for the collector and AI readiness). It is a real button with aria-expanded; Escape, a click outside, a link, and back close it. Phones keep the bottom bar's warning drawer. | fork-only |
