| UI118 | Live tiles carry their camera name by default | hunks in `web/src/views/settings/UiSettingsView.tsx`, `web/src/views/live/LiveDashboardView.tsx`, `web/src/views/live/DraggableGridLayout.tsx` | `displayCameraNames` shipped `false`, so an out-of-box Live wall was anonymous: nothing on the page said which door a tile was pointed at, and the tiles are ordered by config file, so the only way to identify one was to recognise the view. The default is now `true` in all three places the literal appears. No new markup: the tile already draws the chip when the flag is set. | fork-only |
