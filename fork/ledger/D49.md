| D49 | e2e reliability | `fork/scripts/check.sh`, `web/e2e/helpers/app-ready.ts`, `web/e2e/fixtures/frigate-test.ts`, `web/e2e/pages/base.page.ts`, `web/e2e/specs/_meta/app-ready.spec.ts`, `web/src/components/navigation/PageLoading.tsx`, `web/e2e/playwright.config.ts`, `web/scripts/fork/e2e-flaky-report.mjs`, `.github/workflows/fork-checks.yml`, `web/src/hooks/use-optimistic-state.ts`, `web/src/hooks/use-optimistic-state.test.ts` | Two e2e tests passed only on the CI retry and which two varied by run. Most of the victims were the harness, one was the product, and the retry kept all of them quiet. `make check` ran the whole 774-test Playwright suite at 4 workers beside the Docker Python gate, which owns a 4 CPU, 6 GiB VM and starts mypy, the API spec check and the unit tests at once; under that contention assertions hit the 5 s expect timeout with "element(s) not found" in whichever spec happened to be running, and a baseline run here lost `review.spec.ts:92` on mobile while the same suite alone passes. The e2e gate now waits for the build and the Docker lane and has the machine to itself, which puts one e2e run on the critical path that used to overlap the Docker lane, 216 s to 339 s in the runs here. Whole-run totals do not isolate that cost and should not be read as it: they were 2,428 s before and 1,877 s and 1,156 s after, swamped by the Python gate's own spread of 2,425 s, 1,574 s and 926 s across those same three runs. Second cause: `frigateApp.goto()` waited only for `#pageRoot`, which the app shell renders while the route inside it is still suspended on its lazy chunk or on one of the 32 i18n namespaces `i18next-http-backend` fetches over HTTP, so a slow locale response reached the test as a missing element rather than as a page still loading, and thirteen call sites open-coded the same two lines, one of them asserting `toHaveCount(0)` against the loading fallback. Every navigation and reload now goes through `waitForAppReady`, which waits for the route content to replace the fallback; `PageLoading` carries the marker it waits on and no longer suspends on its own namespace, and a `_meta` spec holds the contract by delaying the locales 2 s. Third cause, and the only product bug: `review.spec.ts` "switching back to Alerts works" clicks two severity tabs in a row, and `useOptimisticState` renders the click at once but hands it to the owner after a delay, which for `severity` is a router navigation, so a second click landing between the push and the owner reporting it back was discarded when that echo arrived and the UI stayed on the tab the user had just left. Throttling the CPU 20x reproduces it in about half of the runs and never after the fix, which is to ignore the echo of a value the hook itself pushed and has since superseded, and a new unit test covers the window in both directions. Fourth, the reporting: Playwright writes one JSON report per run, `e2e-flaky-report.mjs` reads them back, annotates every flaky test with its file, line, project and cause, and fails a push to `next`, so a green run that needed the retry is no longer silent | candidate (the `use-optimistic-state.ts` fix; the rest is fork-only) |
