| UI135 | web + backend: PTZ controls in the Add Camera wizard | `frigate/api/camera.py` (`pan_tilt_supported` in `/onvif/probe`), `frigate/ptz/onvif.py` (subscribe to camera add/remove), `frigate/test/test_onvif_probe.py`, `frigate/test/test_ptz_onvif.py`, `web/e2e/specs/settings/camera-wizard-ptz.spec.ts` (new); hunks in `web/src/components/settings/CameraWizardDialog.tsx`, `web/src/components/settings/wizard/{Step2ProbeOrSnapshot,Step3StreamConfig,Step4Validation}.tsx`, `web/src/types/cameraWizard.ts`, `web/public/locales/en/views/settings.json` | When the ONVIF probe reports PTZ, Step 3 offers an "Enable PTZ Controls" switch (on by default when continuous pan/tilt is advertised) with pre-filled ONVIF connection details, and the wizard writes the camera's `onvif` section on save. The ONVIF controller now sets up cameras added at runtime and tears down removed ones, so the controls work without a restart. Import resolved against the fork's consolidated `react-icons/lu` import in Step 3; upstream's `record_sub` role hunk does not exist on this base. Unit tests added for the pan/tilt detection helper and runtime removal | backport of upstream #23444 |
