| C20 | web: explore performance | hunk in `web/src/views/search/SearchView.tsx` (`getSearchResultId`, the comment on `onThumbnailClick`); `web/src/hooks/fork/use-bulk-selection.ts` (doc on `getId`) (+ case in `.test.tsx`) | `SearchView` passed an inline `getId` arrow to `useBulkSelection` (UI10). `getId` is a dependency of `selectAll`, `onItemClick`, `selectedItems` and the hook's final `useMemo`, so the returned object was new on every render, `onThumbnailClick` changed identity with it, and `MemoizedSearchThumbnail` never skipped a render, which undid G5. The comment beside it said the callback was stable. `getId` is now a module-level function, the option's doc says it must keep its identity, and the comment says what does change the callback (the selection, the selection mode, the result list). A `renderHook` test asserts that the returned object, `onItemClick` and `selectAll` keep their identity across a re-render with unchanged inputs, and that a real change still produces a new object | fork-only |
