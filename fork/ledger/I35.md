| I35 | tooling: ledger | `fork/scripts/ledger.py` (new, + `test_ledger.py`), `fork/ledger/` (new, with a README), `FORK.md` (rules), `AGENTS.md` (Ledger IDs), `Makefile` (`ledger`), `fork/Dockerfile.test` (copies `FORK.md` and `fork/ledger`) | Every pull request appended its rows to the end of `FORK.md`, so any two open pull requests conflicted there, and with the up-to-date rule on `next` six pull requests cost six rebases and six serial CI runs on 2026-09-17/18. A new row is now a file, `fork/ledger/<ID>.md`, holding one table row, so two pull requests never edit the same lines; `FORK.md`'s table stays as the history and is only edited to update a row it already has. `ledger.py --check` (run by `test_ledger.py` in `make check` and CI) rejects a row file with the wrong name, more or fewer than one row, a cell count other than five, an ID that differs from the file name, or an ID the table already defines; `make ledger` prints both together sorted by ID | n/a (fork-only) |
