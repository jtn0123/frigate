| D57 | backend + web: GenAI review options from upstream 0.19 | `frigate/config/camera/review.py` (`ReviewResponseStyleEnum`, `response_style`), `frigate/genai/{__init__,prompts}.py`, `frigate/data_processing/post/review_descriptions.py`, `frigate/test/test_genai_prompts.py`, `testing-scripts/genai_review_tester.py` + `genai-review-examples/README.md`, `.gitignore`, docs `review_summaries.md` + `reference.md`, generated `web/public/locales/en/config/{global,cameras}.json` | Review descriptions gain a `response_style` preset (default, natural, concise, detailed) that rewords the title, summary and scene guidance, plus a local prompt tester. Cherry-picked from upstream's 0.19 `dev` with `-x`; the fork's GenAI changes (D18 Ollama token sizing) are untouched | backport of upstream #24166 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
