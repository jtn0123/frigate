| D57 | backend + web: GenAI review options from upstream 0.19 | `frigate/config/camera/review.py` (`ReviewResponseStyleEnum`, `response_style`), `frigate/genai/{__init__,prompts,manager}.py` (`role_info`), `frigate/data_processing/post/review_descriptions.py` (`regenerate_description`, `start_analysis`, `get_recording_buffer_extension`), `frigate/api/review.py` (`PUT /review/{id}/regenerate_description`), `frigate/api/app.py` (`GET /genai/roles`), `frigate/comms/embeddings_updater.py`, `frigate/embeddings/__init__.py`, `web/src/hooks/use-review-descriptions.ts` (+ `.test.tsx`), `web/src/components/filter/ReviewActionGroup.tsx`, `web/src/types/chat.ts`, keys in `web/public/locales/en/components/dialog.json`; `frigate/test/{test_genai_prompts,test_review_description_regenerate}.py`, `frigate/test/http_api/test_http_review_regenerate.py`; `testing-scripts/genai_review_tester.py` + `genai-review-examples/README.md`, `.gitignore`, docs `review_summaries.md` + `reference.md`; generated `docs/static/frigate-api.yaml`, `web/src/types/fork/api.gen.ts`, `web/public/locales/en/config/{global,cameras}.json` | Review descriptions gain a `response_style` preset (default, natural, concise, detailed) that rewords the title, summary and scene guidance, plus a local prompt tester; admins can re-run a single finished review item through GenAI descriptions from the Review selection bar (shown when the descriptions model has at least 32k context) or the API, always from recordings. Cherry-picked from upstream's 0.19 `dev` with `-x`; the regenerate handler follows G3 (the review lookup and the blocking embeddings request run in `asyncio.to_thread`), the analysis keeps next's labelmap arguments, and the fork's GenAI changes (D18 Ollama token sizing) are untouched | backport of upstream #24166, #24222 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
