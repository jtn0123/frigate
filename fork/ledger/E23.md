| E23 | backend: security backports from upstream 0.19 | hunks in `frigate/api/auth.py` (`auth`), `frigate/output/preview.py` (`is_camera_preview_frame`, `get_most_recent_preview_frame`), `frigate/record/export.py` (`save_thumbnail`, `get_preview_export_command`) and `frigate/api/preview.py` (`get_preview_frames_from_cache`); `frigate/test/http_api/test_http_auth_jwt_role.py` (upstream), `frigate/test/test_preview_frame_camera_match.py` (new), a case in `frigate/test/test_preview_loader.py` (upstream) | `/auth` trusted the role inside a JWT and re-signed it on refresh, so a session whose restricted role had since been removed from `auth.roles` kept it, and the camera checks treat an unknown role like one with no camera list (all cameras). `/auth` now returns 401 for a role that is not configured, which sends the user back through login. Cached preview frames were still matched by the prefix `preview_{camera}-` in the `latest.jpg` offline fallback, the export thumbnail fallback, the preview export playlist and the `/preview/.../frames` listing, so camera `front` could pick up frames of `front-door` (the `latest.jpg` fallback could serve them to a user without access to that camera). All four now use upstream's `is_camera_preview_frame`, which compares the whole name before the last "-" as D33 and G15 already do; upstream still prefix-matches in the last two, so those hunks go beyond #24352 | backport of upstream #24352 |
