| I51 | Class balance | `frigate/fork/classification_suggestions.py` (`dataset_counts`, `dataset_balance`, `would_unbalance`), `frigate/fork/classification_prefetch.py`, `frigate/api/fork_classification_suggestions.py` (`dataset` in the report); `web/src/pages/fork/SuggestionReportPage.tsx` (images per class), `web/src/components/fork/classification/SuggestionStatusBar.tsx` (warning chip); tests, e2e cases | Upstream's docs say to keep the largest class within about three times the smallest. Nothing showed the counts, and auto-filing could widen the gap on its own. The report lists images per class with empty classes apart, the status line warns when the ratio is past 3, and auto-filing never files a class past that ratio | n/a (fork-only) |
