| UI133 | The e2e suite matches the behavior UI118-UI132 shipped | hunks in `web/e2e/specs/config-editor.spec.ts`, `web/e2e/specs/explore.spec.ts`, `web/e2e/specs/settings/ui-settings.spec.ts`, `web/e2e/specs/fork/phone-touch.spec.ts`, `web/e2e/specs/fork/state-polish.spec.ts` | The UI walk changed behavior the suite still asserted the old shape of. The five Save & Restart tests clicked a button that UI121 now keeps inert until the buffer is dirty, so each one types an edit first. The Explore header count moved into a chip under UI119, so the assertions read its `aria-label` rather than the old sentence, and the tile selector follows `aspect-square` to `aspect-video`. The empty-metric cards gained a named subject and a last-updated line under UI124, so the state-polish assertions match on substrings. One stale desktop assertion in the UI settings switch test expected the second click to leave the toggle off. | fork-only |
