| UI136 | web: export and import browser-stored UI settings | new `web/src/utils/uiSettingsTransfer.ts` (+ `.test.ts`), `web/src/components/overlay/dialog/ImportUiSettingsDialog.tsx`, `web/e2e/specs/settings/ui-settings-transfer.spec.ts`; hunks in `web/src/views/settings/UiSettingsView.tsx`, `web/public/locales/en/views/settings.json`, `docs/docs/usage/live.md` | Camera group layouts, per-camera streaming settings and UI preferences live in IndexedDB, so they were stuck on one browser. A Backup & Restore card in UI Settings exports them to a JSON file and imports it on another device through a confirmation dialog with per-section switches and warnings for unknown groups and cameras; only registry-known keys that pass their zod schema are written, and streaming settings merge per camera. Conflict resolved so the fork's loaded-flag persistence (UI118 default names on, UI122/UI123 destructive clear buttons and confirmation dialog) is kept beside the new card. Fork adaptations: async handlers wrapped in `wrapAsync` (no-misused-promises), upstream's 0.19-only `recordingQuality` key left out of the registry, the e2e describe tagged `@mobile` for the spec lint, and 11 vitest cases for the transfer module | backport of upstream #24025 |
