| UI113 | Settings: Frigate+ status and page header insets | new `web/src/components/fork/settings/{StatusPill,PlusKeyStatus}.tsx`; `web/public/locales/en/fork.json`; hunks in `web/src/views/settings/FrigatePlusSettingsView.tsx`, `web/src/views/settings/UiSettingsView.tsx` | Frigate+ showed red X icons for "no API key" and "snapshots off", which read as errors. They are neutral pills ("Not connected", "Off"), with a line saying how to connect: set `PLUS_API_KEY` as an environment variable or Docker secret (Home Assistant app: the Frigate+ API key option), then restart; this matches `frigate/plus.py`, and `plus.enabled` means a well-formed key is present. An "Open snapshot settings" button links to Snapshots. The Frigate+ and UI settings titles sat 8 px lower than config-form titles (an extra `pt-2` / `mt-2`); both now line up, and E2E checks UI settings, Media sync and Frigate+ titles against Recording within 1 px. | fork-only |
