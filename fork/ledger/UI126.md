| UI126 | The live layout toggles are named, translated and announce which is on | hunks in `web/src/views/live/LiveDashboardView.tsx`, `web/public/locales/en/fork.json` | The phone header carried two adjacent buttons that differ only in the arrangement of bars inside their icons, and both took hard-coded English `aria-label`s ("Use mobile grid layout", "Use mobile list layout", "Enter layout editing mode") in a codebase where user-facing strings go through i18n. The labels are translated now, and each button carries `aria-pressed` so assistive tech says which layout is active rather than offering two unrelated commands. | fork-only |
