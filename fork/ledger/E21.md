| E21 | security: share links | hunks in `docker/main/rootfs/usr/local/nginx/conf/nginx.conf` (`error_log` in the share location and in `@fork_share_authed`); cases in `fork/scripts/test_nginx_share_conf.py` | E15 masked the share token in the access log and lowered the limit refusals to `info`, but an upstream timeout or a client closing early is written to the error log at `error` level with the raw request line, token included. Both share locations now set `error_log /dev/stdout crit;` (the path the file already uses), so only `crit` and above are written for these paths. The authenticated location is included because `DELETE /api/fork/share/<token>` carries the token in its request line too. Residue: a `crit` or higher line for a share request would still print the token, and the share page itself (`/share/<token>`, served by `location /`) keeps the global `warn` level | fork-only |
