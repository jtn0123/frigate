| UI122 | The two Clear All buttons read as destructive | hunks in `web/src/views/settings/UiSettingsView.tsx` | "Clear All Layouts" discards every camera arrangement that has been dragged into place and "Clear All Streaming Settings" every per-group streaming choice. Both wore the same quiet default variant as Copy Config, and both act on browser local storage, so there is no server copy to restore from. They now take `variant="destructive"`, which the button component already defines. This is the warning only: both still act on the first click, and the confirmation is deliberately left for a follow-up so the choice between a dialog and an undo in the toast can be made on its own. | fork-only |
