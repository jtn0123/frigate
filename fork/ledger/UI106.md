| UI106 | UI: recording view timeline rail | new `web/e2e/specs/fork/timeline-rail.spec.ts`, `web/src/components/timeline/__tests__/timeline-zoom-controls.test.tsx`; `web/src/lib/fork/timeline-scrubber.ts`, `web/public/locales/en/fork.json`; hunks in `web/src/components/timeline/ReviewTimeline.tsx`, `web/src/components/timeline/segment-metadata.tsx`, `web/src/components/timeline/VirtualizedMotionSegments.tsx`, `web/src/components/timeline/VirtualizedEventSegments.tsx`, `web/src/views/recording/RecordingView.tsx` | On the desktop recording view the zoom buttons sat on the bottom of the rail and covered the red current-time pill, which was also cut in half: the rail only scrolled to follow the current time once its segment left the viewport, so a segment on the last row counted as visible. The rail now recenters when the followed segment comes within 40 px of either edge, on desktop and phone (at the very end of the range it cannot scroll further, and the pill now fits there). On desktop the zoom buttons are a bordered group in a bar above the rail, which starts below it. Desktop tick labels go from 8 px to 11 px medium; phones keep 10 px, since 11 px overflows the 65 px phone Review rail. In dark they use `primary-variant` (6:1 on the default rail instead of 4.6:1). The Timeline / Events / Detail switch is one segmented control with the active mode raised, like the phone tabs. The zoom buttons gain accessible names. | fork-only |
