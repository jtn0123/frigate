| UI106 | UI: recording view timeline rail | new `web/e2e/specs/fork/timeline-rail.spec.ts`, `web/src/components/timeline/__tests__/timeline-zoom-controls.test.tsx`; `web/src/lib/fork/timeline-scrubber.ts`, `web/public/locales/en/fork.json`; hunks in `web/src/components/timeline/ReviewTimeline.tsx`, `web/src/hooks/use-draggable-element.ts`, `web/src/components/timeline/segment-metadata.tsx`, `web/src/components/timeline/VirtualizedMotionSegments.tsx`, `web/src/components/timeline/VirtualizedEventSegments.tsx`, `web/src/views/recording/RecordingView.tsx` | On the desktop recording view the zoom buttons sat on the bottom of the rail and covered the red current-time pill, which could also hang off the rail's end. The follow scroll ran before the pill was placed, and the pill adds scroll height at the oldest end of the range; the second scroll meant for that case never ran on the motion rail, which only finds aligned segment times. It now gets the aligned time. The rail also recenters when the followed segment comes within 40 px of either edge, instead of only once it leaves the viewport. On desktop the zoom buttons are a bordered group in a bar above the rail, which starts below it. Desktop tick labels go from 8 px to 11 px medium; phones keep 10 px, since 11 px overflows the 65 px phone Review rail. In dark they use `primary-variant` (6:1 on the default rail instead of 4.6:1). The Timeline / Events / Detail switch is one segmented control with the active mode raised, like the phone tabs. The zoom buttons gain accessible names. Keyboard moves of the playhead or export handles (Home, End, arrows, Page keys) also scroll the rail to the new time, since the draggable hook skipped its follow for 3 s after any scroll. | fork-only |
