| UI137 | web: pick recognized plates in the LPR known plates editor | `web/e2e/specs/settings/lpr-known-plates.spec.ts` (new); hunks in `web/src/components/config-form/theme/fields/KnownPlatesField.tsx`, `web/public/locales/en/views/settings.json` | Each known-plate entry is now a combobox listing plates Frigate has already recognized (`/recognized_license_plates?split_joined=1`, fetched only while the card is open), with the entry each plate is already assigned to, and it still accepts free text so a regex stays typeable. A newly added plate row opens its picker. Applied cleanly; the fork adds a desktop and phone e2e case | backport of upstream #24059 |
