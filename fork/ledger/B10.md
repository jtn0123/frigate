| B10 | backend: camera watchdog | hunk in `frigate/video/ffmpeg.py` (`CameraWatchdog.run`: an expired switch calls `reset_capture_thread(cause="hwaccel retry")`); cases in `frigate/test/test_hwaccel_fallback.py` | B5 sent an expired software-decoding switch through the branch for an `ffmpeg` config update: `stop_all_ffmpeg()` and `start_all_ffmpeg()`, a reset of `record_enable_time`, and a debug line that said "FFmpeg config updated". A camera that had fallen back got a gap in its recordings every 7 days, and again each week while hardware decoding kept failing. Only the detect process decodes, so expiry now restarts only that one, the way the switch itself did when it activated: the record process is never stopped, the segment times and `record_enable_time` stay as they were, and an info line says detect restarts to retry hardware decoding. The restart is written to the restart log with the cause "hwaccel retry". The upstream condition is back to `"ffmpeg" in updates`. The B5 test that expected a second `start_all_ffmpeg()` now expects one, no `stop_all_ffmpeg()`, and the detect reset; a second test runs the loop with real process bookkeeping and checks the record process object is the same one and was never terminated. Found by a code grade | candidate |
