| B11 | backend: fork updates, bulk delete, share links | hunks in `frigate/fork/updates.py` (`_refresh`), `frigate/api/fork_bulk.py` (`delete_events_data`) and `frigate/api/fork_share.py` (`_create_link_within_cap`, `_create_lock`); cases in `frigate/test/test_fork_updates.py`, `frigate/test/http_api/test_http_event.py`, `frigate/test/http_api/test_http_fork_share.py` | Three edge cases in code merged the week before. When the release fetch raised something other than the two errors it handles, `_checked_at` stayed unset, so every `/fork/updates` poll asked GitHub again with no backoff; every outcome now counts as a check and the state reports `error: "failed"` until the next one (the exception still reaches the caller, and the B6 test now waits the interval before its second fetch). Bulk event delete unlinked the snapshots before it deleted the rows, the opposite of `delete_reviews_data`, so a failed query left events without media; rows and embeddings go first now, and a snapshot that cannot be removed is logged as a warning instead of failing a request whose rows are already gone. The 50 active links per user were counted and the link inserted as two steps, so parallel POSTs could all count 49 and go over the cap together; both now happen under one lock. It is a lock and not the `database.atomic()` the grade report asked for, because Frigate's `SqliteQueueDatabase` raises `ValueError` on `atomic()`, and every create runs in the one API process. Found by a code grade | fork-only |
