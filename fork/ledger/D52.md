| D52 | browser tests | `web/e2e/specs/fork/rail-search.spec.ts` | Target the palette input in mobile rail-search tests so the drawer-to-dialog focus handoff cannot drop the start of a page-level typing sequence; keep the letter-by-letter debounce check on the input itself. | fork-only |
