| D55 | backend: face recognition revamp from upstream 0.19 | `frigate/data_processing/common/face/detector.py`, `frigate/data_processing/common/face/recognizer.py` (renamed from `model.py`), `frigate/test/test_face_detector.py`, `fork/Dockerfile.test` (clears the base image's `frigate/` and `migrations/` before copying, so the renamed `model.py` does not linger in the test image); hunks in `frigate/data_processing/real_time/face.py`, `frigate/embeddings/__init__.py` (`EmbeddingMaintainer` imported inside `run`, still after the D17 preload), `frigate/log.py` (typed `redirect_output_to_logger`), `frigate/test/test_transcription_preload.py` (patch target moved) | Faces were aligned by rotating on the eye line from the 68 point landmark model, which let scale and position slip on the small faces a camera sees. Face detection now lives in its own `FaceDetector` that returns YuNet's 5 landmarks, faces are warped onto the ArcFace 5 point template, landmark sets that do not fit a face shape are rejected (falling back to the landmark model on tight crops), cut off faces no longer wrap to huge boxes, and detector calls are locked because the recognizer builds its class means on a background thread. Model files and download URLs are unchanged | backport of upstream #24236 |
