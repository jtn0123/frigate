| I47 | Suggestion report page | `web/src/pages/fork/SuggestionReportPage.tsx` (+ test), hunk in `web/src/App.tsx` (route `/classification/suggestions/:model`), `web/src/components/fork/classification/SuggestionStatusBar.tsx` (Report link); `web/e2e/specs/fork/classification-suggestion-report.spec.ts`; `fork/CLASSIFICATION-SUGGESTIONS.md` | The report from I42 and I45 only surfaced as hover tooltips on the status line, which phones cannot hover and which cannot hold the camera and source splits or the latest disagreements. The page reads the same report endpoint and lays it out in plain words, with a one-line explanation under every number and table: the headline numbers (reviewed, "Guesses you accepted", "Added automatically", "Model agrees with descriptions", new since training), the "Accept all" count as "Accepted in bulk (not counted)" overall and per class, tables by guessed class (with what drafts were changed to, how many were added automatically and how many in bulk), by camera and by source, the trained model's classes against what the descriptions said, and the latest disagreements each linked to the event in Explore. Reached from the Report link on the status line. Admin routes only, like the rest of classification | n/a (fork-only) |
