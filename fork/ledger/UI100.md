| UI100 | UI: phone touch targets and layout | new `web/src/lib/fork/{phone-target,phone-scroll}.ts`, `web/src/components/fork/CommandPaletteMenuItem.tsx`, `web/e2e/specs/fork/phone-touch.spec.ts`, `web/e2e/helpers/phone-palette.ts`; `web/src/lib/fork/{phone,phone-shell,phone-fullscreen}.ts`, `web/src/components/fork/{ForkNavItems,ErrorState,PhoneTimelineTabs}.tsx`, `web/themes/fork-appearance.css`; hunks in `web/src/components/ui/{button,sonner}.tsx`, `web/src/components/menu/GeneralSettings.tsx`, `web/src/components/mobile/MobilePage.tsx`, `web/src/components/dynamic/CameraFeatureToggle.tsx`, `web/src/components/filter/SearchFilterGroup.tsx`, `web/src/components/overlay/detail/{SearchDetailDialog,DetailActionsMenu}.tsx`, `web/src/components/timeline/{ReviewTimeline,segment-metadata}.tsx`, `web/src/components/player/PlayerStats.tsx`, `web/src/components/settings/PolygonDrawer.tsx`, `web/src/components/auth/AuthForm.tsx`, `web/src/components/chat/ChatComposer.tsx`, `web/src/pages/{System,Logs,ConfigEditor}.tsx`, `web/src/views/{events/EventView,explore/ExploreView,live/LiveCameraView}.tsx`, `web/src/login.tsx` | A phone audit (412 x 915, Android Chrome) measured 297 controls under 40 px across 14 views; after this, 123, of which about 105 are the Review timeline's 8 px scrub rows, which are not tap buttons. The bottom bar held eight entries at 36 px: the command palette button moved into the Settings drawer on phones (tablets keep it) and the bar's portrait side insets went from 16 to 8 px, so seven entries fit at 48 px on a 412 px phone (a 360 px phone fits six, so it falls back to 36 px while the warning icon shows). The shared Button, the System, Review and Logs tab strips and the camera feature toggles get a 44 px minimum on phones; the fullscreen rail opts out and stays 40 px (its e2e test caught the first attempt). Bugs found on the way: the camera settings cog was titled "[object Object] Settings" (upstream passed the config object to `t()`); Explore thumbnails sat half their height too low and left their card, because the image was `absolute` with no `top` inside a `<button>`, which centers its content (from the fork's own a11y commit 8e154085e, all layouts); the System header was squeezed back to `min-h-11` by its flex column, so its wrapped second row drew over the page title. Also: the open System tab is named on a phone; Config editor buttons carry their labels in one text row; the Explore filter row and Logs tab strip fade at the cut-off edge; timeline labels 8 to 10 px and live stats 9 to 11 px; toasts default to top-center on phones, off the bottom bar; ErrorState no longer repeats the generic sentence as its detail; login fields have `autoComplete` and the login page gets the Android zoom unlock; zone editor vertices have a 44 px grab area; 44 px hit areas for the inline edit pencils and the "more" dots; `prefers-reduced-motion` is honored in CSS (spinners excepted) and by the two framer-motion slide-ins through `useReducedMotion`. `MotionConfig` in the app provider was tried first and dropped: it pulled framer-motion into the eager bundle (+49 KB gzip, over budget). Not done: viewport-driven layout in place of user-agent detection (about 660 references in upstream files) | fork-only |
