| UI121 | The config editor's safe write is the filled button, and neither saves until you type | hunks in `web/src/pages/ConfigEditor.tsx` | Save & Restart and Save Only were the same default variant, the same size, 8 px apart, and both were enabled before anything was typed, so the button that stops recording and restarts the service was indistinguishable from the one that writes a file. Save Only now takes `variant="secondary"`, the one filled button in the row, and both are disabled while the buffer is clean. `hasChanges` was already tracked for the navigation guard and is now read here too. Save & Restart already opened `RestartDialog`, so the confirmation was not the gap. | fork-only |
