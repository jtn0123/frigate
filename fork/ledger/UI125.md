| UI125 | Settings rows stop speaking two languages for "tap me" | hunk in `web/src/pages/Settings.tsx` | The settings list mixed two treatments: a single-item group rendered as a bold navy row with no chevron, a multi-item group as a grey row with one. All were tappable, but bold text above a list reads as a heading everywhere else, so the leaf pages looked like section titles for the rows beneath them. The group trigger now takes the same weight, colour and padding as a row, leaving the chevron alone to say "there is more inside". | fork-only |
