| D48 | coverage gates | `.coveragerc`, `web/vite.config.ts`, `web/src/lib/fork/settings-save.test.ts` | Add Python and web coverage floors calibrated to measured local baselines, with stronger fork-library floors; new transaction tests improve covered behavior. The existing Sonar 80% new-code gate remains and requires a fresh CI run to establish recovery. | fork-only |
