| UI124 | An empty System card names the series it waits on and warns when it is stale | hunks in `web/src/components/fork/MetricEmptyState.tsx`, `web/src/components/fork/MetricEmptyState.test.tsx`, `web/src/views/system/GeneralMetrics.tsx`, `web/public/locales/en/fork.json` | Every empty metric card printed the same sentence in the same dashed box, so a detector that had gone silent and one that had simply not reported yet were indistinguishable, and neither said how long the wait had been. The card now names the series it would draw, prints how long ago the last stats payload arrived, and once stats have been flowing for 30 seconds without this series it takes the danger border and says the series has not reported. Four tests cover the named, stale and short-wait states and the clamp that keeps the elapsed count non-negative. | fork-only |
