| UI123 | The Clear All buttons ask before discarding | new `web/src/components/fork/settings/ConfirmClearDialog.tsx`; hunks in `web/src/views/settings/UiSettingsView.tsx`, `web/public/locales/en/fork.json` | UI122 made the two buttons read as destructive; they still fired on the first click. Both now open an `AlertDialog` that names what is lost and says the data lives in this browser only, so there is no copy to restore from. The dialog is one component with the title, sentence and action passed in, so a third clear-all would reuse it. | fork-only |
