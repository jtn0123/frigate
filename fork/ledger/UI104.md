| UI104 | Settings: Triggers prerequisite notice | new `web/src/components/fork/settings/TriggersSemanticSearchNotice.tsx`; `web/public/locales/en/fork.json`; hunk in `web/src/views/settings/TriggerView.tsx` | Semantic search being off is a missing prerequisite, not an error, but Triggers showed it as a red destructive alert. It is now a neutral info notice ("Triggers need semantic search") that says why, with a primary button to the Semantic search settings page and the existing documentation link. The upstream `triggers.semanticSearch.*` strings are left in place, unused. | fork-only |
