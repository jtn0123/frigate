| UI119 | Explore shows the whole frame and states the count once | hunks in `web/src/views/explore/ExploreView.tsx` | Each Explore row printed its size in the header ("2 Tracked Objects") and again in the link at the far end of the same line ("View all 2"), 1,600 px apart. The tiles were `aspect-square`, so every 16:9 frame was centre-cropped and the left and right thirds of the scene, where a person usually enters, were cut off before you could judge the clip. The count is now a chip beside the row title, the link reads "View all", and the tile is `aspect-video`. The translated sentence survives as the chip's `aria-label`, so a screen reader still hears "2 tracked objects". | fork-only |
