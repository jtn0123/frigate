| I52 | Spot check of auto-filed images | `frigate/fork/classification_suggestions.py` (`recent_auto_filed`, `spot_check`), `frigate/api/fork_classification_suggestions.py` (`POST /classification/{name}/suggestions/spot-check`, `recent_auto_filed` in the report), request and response models; `web/src/hooks/fork/use-spot-check.ts`, `web/src/pages/fork/SuggestionReportPage.tsx`; tests, e2e case; `docs/static/frigate-api.yaml` | Auto-filed images (I44) were only ever a count. A wrong description filed a wrong label with nobody looking. The report page now shows the latest auto-filed groups nobody has checked, each with its image, class, camera and time. Keep records an accepted draft. Remove asks first ("Remove this photo from training?", Cancel or Remove), since it cannot be undone, then deletes the images and records a rejected one. Both feed the kept rate, so a class that keeps failing its spot checks loses auto-filing by itself | n/a (fork-only) |
