| I37 | CI, tooling | `fork/scripts/ci-changes.sh` (+ `test_ci_changes.py`, new), `fork/scripts/check.sh`, `fork/scripts/sonar-token-expiry.py` (+ tests), `.github/workflows/fork-checks.yml` (the expiry step gets `SONAR_TOKEN`) | Three gaps in the fork's own gates. The change detector did not match `check.sh`, `promote.sh` or any other shell script under `fork/scripts/`, nor `fork/sonar-token.env`, so a pull request that broke them skipped every suite and went green; they now run everything, and a `fork/ledger/` row runs the Python jobs that validate it. `check.sh`'s stale-checkout warning (I32) trusted `origin/next` as of the last fetch, so the unfetched clone it was written for counted 0 behind; it now asks the remote where `next` is (read-only, skipped offline) and says to fetch when the ref has moved. After the documented expiry the Sonar check failed every pull request even if the token had been rotated and only the date file forgotten; it now asks SonarCloud whether the token still authenticates and only warns about the stale date when it does | n/a (fork-only) |
