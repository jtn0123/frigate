| D63 | plate picker focus | KnownPlatesField and browser regression | Seed and select the plate search during popover autofocus so fast typing cannot lose the first character to a delayed animation-frame selection. Reproduced while testing the upstream integration. | fork |
