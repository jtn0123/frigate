| UI111 | Settings: Camera management table and row delete | new `web/src/components/fork/settings/{CameraStateLegend,DeleteCameraRowButton}.tsx`; `web/public/locales/en/fork.json`; hunks in `web/src/views/settings/CameraManagementView.tsx`, `web/src/components/overlay/dialog/DeleteCameraDialog.tsx` | A red "Delete Camera" button sat beside Add and Clone, and the state list repeated a paragraph of On/Off/Disabled help. Delete is now a per-row trash button that opens the existing confirm dialog on that camera (the dialog takes an optional `camera` and its Back becomes Cancel); Add stays primary and Clone is secondary with its description as a tooltip. The state list is a table (camera, state, actions) that keeps drag-to-reorder and rename, and a short legend replaces the help text. The legend copy was checked against the code: Off sends the runtime off command, which does not survive a restart; Disabled writes `enabled: false` and needs a restart to turn back on. | fork-only |
