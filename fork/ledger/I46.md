| I46 | Draft class in the Explore detail dialog | `frigate/api/fork_classification_suggestions.py` (`/classification/suggestions/event/{event_id}`, `draft_events`, `event_models`, `filed_entry`), `frigate/api/defs/response/fork_classification_suggestions_response.py`; `web/src/components/fork/classification/ExploreSuggestion.tsx`, `web/src/hooks/fork/use-event-suggestions.ts`, `web/src/lib/fork/classification-suggestions.ts`, hunk in `web/src/components/overlay/detail/SearchDetailDialog.tsx` (`ObjectDetailsTab`); `web/e2e/specs/fork/classification-explore-suggestion.spec.ts`; tests; `fork/CLASSIFICATION-SUGGESTIONS.md` | Filing a class meant leaving the event and finding its card on the training page. The detail dialog now asks one endpoint for the event and shows, per custom model that classifies its label, the draft and its source, what the trained model called it, and what it was already filed as. An Add button files the event's waiting train images through the same confirm endpoint, so provenance and the report see it. The line uses plain words: the model's name without underscores, "Guess: suv", "97% sure", "Add as suv" (named per model for screen readers), "In training as suv" and "Your model thinks: sedan" without alarm color. The button stays busy until the line has refreshed, a 404 "already accepted" reads as "Already sorted", any other failure shows the server's message, and every outcome refreshes the line. Admins only, like the training page | n/a (fork-only) |
