| UI130 | Date presets line up, the System header clears the window edge, chat suggestions sit on a grid | hunks in `web/src/components/ui/calendar-range.tsx`, `web/src/pages/System.tsx`, `web/src/components/chat/ChatStartingState.tsx` | Three layout fixes found in the same walk. The date-range popover's preset column was `items-end`, so nine labels of different lengths formed a ragged left edge and the selected-item tick floated; the column is `items-stretch` and each preset is a left-aligned row. The System page header had no right padding, so "Last refreshed: Just now" sat flush against the window edge at 1920 px. The chat starting state wrapped four suggestion buttons of four different widths with `justify-center`, making a lopsided two-row block; they are a two-column grid with one width and one left edge, keyed by prompt rather than by array index. | fork-only |
