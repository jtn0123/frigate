| I54 | Retrain nudge on the report | `frigate/fork/classification_suggestions.py` (`training_gap`), `frigate/api/fork_classification_suggestions.py` (`training` in the report); `web/src/pages/fork/SuggestionReportPage.tsx`; tests, e2e case | Upstream's Train button counts new images, but the report page, where auto-filed images and spot checks are reviewed, said nothing about whether the work had reached the model. It now shows the images added since the last training (or that the model has not been trained yet), read from upstream's training metadata beside the dataset | n/a (fork-only) |
