| UI102 | Settings: "On this page" rail never covers the form | `web/src/components/fork/settings/SettingsNav.tsx`, new `web/e2e/specs/fork/settings-polish.spec.ts`; hunk in `web/src/pages/Settings.tsx` | At 1440 px the rail sat on top of the right edge of the form's cards. The rail now shows only at 1440 px and wider (`2xl`), and while it is rendered the desktop scroll area reserves a 15rem right gutter (`2xl:has-[[data-settings-rail]]:pr-60`) that it sits in, so it cannot overlap anything; below 1440 px it is hidden. E2E checks the rail box against the form and every field group card at 1440, 1536 and 1920 px, and that it is hidden at 1280 px and on phones. | fork-only |
