| I41 | Classification suggestions | `frigate/fork/classification_suggestions.py`, `frigate/api/fork_classification_suggestions.py`, `frigate/config/fork/classification_suggestions.py`, `frigate/api/defs/{request,response}/fork_classification_suggestions_*.py`, hunks in `frigate/config/classification.py` (`suggestions` field), `frigate/api/fastapi_app.py` and `generate_api_auth_spec.py` (router registration); `web/src/components/fork/classification/SuggestionBadge.tsx`, `web/src/hooks/fork/use-classification-suggestions.ts`, `web/src/hooks/fork/use-confirm-suggestion.ts`, `web/src/lib/fork/classification-suggestions.ts`, hunks in `web/src/views/classification/ModelTrainingView.tsx` (`ObjectTrainGrid` badge and class picker), `classificationSuggestions` flag; `fork/CLASSIFICATION-SUGGESTIONS.md` | The train grid of a custom object model showed each event's images with no hint, even though the event's description usually names the class. `GET /classification/{name}/suggestions` drafts one of the model's dataset classes per event from that description, locally and, when `classification.suggestions.jev.enabled` and an API key are set, through Jev on OpenRouter (text only, cached per description, capped per day). The card shows the draft with its source and score; Confirm files every image of the event under it through `POST .../suggestions/confirm`, and the class picker on such a card files through the same call, which records the suggestion beside the dataset in `.fork_provenance.jsonl` so accuracy can be measured later. Nothing is labeled without a click | n/a (fork-only) |
