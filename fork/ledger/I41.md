| I41 | Classification suggestions | `frigate/fork/classification_suggestions.py`, `frigate/api/fork_classification_suggestions.py`, `frigate/config/fork/classification_suggestions.py`, `frigate/api/defs/{request,response}/fork_classification_suggestions_*.py`, hunks in `frigate/config/classification.py` (`suggestions` field), `frigate/api/fastapi_app.py` and `generate_api_auth_spec.py` (router registration); `web/src/components/fork/classification/SuggestionBadge.tsx`, `web/src/hooks/fork/use-classification-suggestions.ts`, `web/src/hooks/fork/use-confirm-suggestion.ts`, `web/src/hooks/fork/use-suggestion-hint.ts`, `web/src/lib/fork/classification-suggestions.ts`, hunks in `web/src/views/classification/ModelTrainingView.tsx` (`ObjectTrainGrid` badge and class picker) and `web/src/components/card/ClassificationCard.tsx` (`labelSlot` prop on the card and the grouped card), `classificationSuggestions` flag; `fork/CLASSIFICATION-SUGGESTIONS.md` | The train grid of a custom object model showed each event's images with no hint, even though the event's description usually names the class. `GET /classification/{name}/suggestions` drafts one of the model's dataset classes per event from that description, locally and, when `classification.suggestions.jev.enabled` and an API key are set, through Jev on OpenRouter (text only, cached per description, capped per day). The card shows the draft in its bottom label row in place of "None" (the class with a question mark, a confidence dot and an accept check), and the class name opens a popover with the score, the source and the matched sentence; the check files every image of the event under it through `POST .../suggestions/confirm`, and the class picker on such a card files through the same call, which records the suggestion beside the dataset in `.fork_provenance.jsonl` so accuracy can be measured later. Every popover lists the classes as one-tap buttons, and a card with no guess shows a "Pick a class" button whose popover says why there is no guess, or "Sedan, maybe" when Jev leaned below the draft gate (score 0.55 and margin 0.1, returned as `maybe`); a maybe is filed only by hand and records no `suggested_category`. Confirming a group whose files are all gone returns 404 `already accepted`, and `POST .../suggestions/undo` moves an accepted group back to the train grid under its original names and records an undo line that takes the confirmation out of the report and the auto-file gate; confirm, undo and auto-filing share one lock per model so a group never half-moves. The grid sends up to 400 events per request and `omitted` says how many were dropped past that. Nothing is labeled without a click. The UI calls a draft a suggestion and confirming it accepting, and a one-line hint above the grid explains the pills until dismissed | n/a (fork-only) |
