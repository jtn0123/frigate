| E20 | security: share links | hunks in `docker/main/rootfs/usr/local/nginx/conf/nginx.conf` (`$fork_share_token` map, `fork_share` and `fork_share_conn` keys, `fork_share_addr` and `fork_share_addr_conn` zones, the share location); cases in `fork/scripts/test_nginx_share_conf.py` | The public share limits (E15) were keyed on `$binary_remote_addr`. Behind a reverse proxy, and always with `FRIGATE_BASE_PATH` (which proxies from 127.0.0.1), every viewer has the same address, so all of them shared one 5 r/s, 8-connection bucket and one viewer could 429 the rest. A `map` on `$uri` now takes the token segment (with or without `/api`, because the limits run after the location's rewrite), and the two strict zones are keyed on it: the limit is per shared clip, the resource being protected, at the same 5 r/s, burst 10 and 8 connections. A second, looser pair of zones stays keyed on the address (30 r/s, burst 60, 48 connections) and bounds a client that walks through many tokens, which a token key cannot. Any other path maps to an empty key, which nginx does not account | fork-only |
