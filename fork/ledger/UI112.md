| UI112 | Settings: Media sync layout and start button | new `web/src/lib/fork/media-sync-selection.ts` (+ test); `web/public/locales/en/fork.json`; `web/src/views/settings/MediaSyncSettingsView.tsx` | The page used its own two-column layout with an extra inset, and while "All Media" was on the per-type switches were disabled but drawn on, so their state was unclear. It is now one column aligned with the other settings pages: a status card, then Media types and Options cards, then the start button. Every type switch stays live; turning one off while All Media is on keeps the others, and selecting all six collapses back to All Media. The button reads "Start dry run" while Dry run is on and "Start sync" otherwise, with a hint saying whether files will be deleted, and it is disabled with a hint when no type is selected (upstream sent an empty list). | fork-only |
