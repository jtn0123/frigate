| D56 | backend + web: GenAI chat improvements from upstream 0.19 | `frigate/api/chat.py`, `frigate/api/chat_util.py`, `frigate/api/defs/request/chat_body.py` (`tool_decisions`), `frigate/api/app.py` (`/categorized_object_names`), `frigate/api/auth.py`, `frigate/genai/prompts.py`, `frigate/util/object_names.py` (new); `web/src/pages/Chat.tsx`, `web/src/components/chat/` (`ToolApprovalCard.tsx` new, `ChatComposer.tsx`, `ChatSettings.tsx`, `ToolCallsGroup.tsx`), `web/src/utils/chatUtil.ts`, `web/src/types/chat.ts`, keys in `web/public/locales/en/views/chat.json`; tests `frigate/test/test_object_names.py` (new), `frigate/test/test_chat_tool_approval.py` (new), `web/src/utils/fork/chatUtil-approval.test.ts`, `web/src/components/chat/fork/ToolApprovalCard.test.tsx`, `web/e2e/specs/fork/chat-approval.spec.ts` | Backport of the 0.19 chat work. Shorter tool descriptions with the filter routing stated once in the system prompt, and a `get_categorized_object_names` tool and endpoint so the model looks up the exact face, plate, logo and classification names before searching by `sub_label`. State-changing tools (`set_camera_state`, `start_camera_watch`, `stop_camera_watch`, `create_export`) now pause for the user's approval, with an "always allow" list kept in the browser; new `get_export_cases`, `create_export` and `get_event_image` tools. Fork differences: `get_export_cases` applies the same case visibility rule as `GET /cases`, and the E11 owner check on `stop_camera_watch` and the UI98 edit lock are kept | backport of upstream #23864, #24173 and 15c41c985 |
