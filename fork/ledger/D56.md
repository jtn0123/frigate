| D56 | backend + web: GenAI chat improvements from upstream 0.19 | `frigate/api/chat.py`, `frigate/api/app.py` (`/categorized_object_names`), `frigate/api/auth.py`, `frigate/genai/prompts.py`, `frigate/util/object_names.py` (new); tests `frigate/test/test_object_names.py` (new) | Backport of the 0.19 chat prompt refinements: shorter tool descriptions with the filter routing stated once in the system prompt, and a `get_categorized_object_names` tool and endpoint so the model can look up the exact face, plate, logo and classification names before searching by `sub_label` | backport of upstream #23864 |
