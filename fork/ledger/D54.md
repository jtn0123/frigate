| D54 | backend + web: upstream 0.19 backports, round 2 | `frigate/track/norfair_tracker.py` (+ `frigate/test/test_norfair_deregister.py`); `frigate/motion/improved_motion.py` (+ `frigate/test/test_motion_detector.py`); `frigate/events/audio.py`, `frigate/app.py` (+ `frigate/test/test_audio_spawn.py`); `frigate/log.py`, `frigate/util/ffmpeg.py`, `frigate/video/restart_log.py`, `frigate/video/ffmpeg.py` (`_restart_stalled_record`) (+ `frigate/test/test_ffmpeg_log_filters.py`, `test_restart_log.py`, `test_record_watchdog.py`); runtime camera add/remove guards in `frigate/track/object_processing.py` (locked `get_camera_state(s)`), `frigate/api/chat.py`, `event.py`, `media.py`, `frigate/comms/webpush.py`, `frigate/data_processing/post/*`, `frigate/embeddings/maintainer.py`, `frigate/output/birdseye.py`, `output.py`, `frigate/ptz/autotrack.py`, `onvif.py`, `frigate/record/maintainer.py`, `frigate/review/maintainer.py` (+ `frigate/test/test_camera_lifecycle.py`, `http_api/test_http_media_access.py`); `frigate/util/file.py` (`trim_oldest_files`), `frigate/data_processing/real_time/face.py`, `custom_classification.py` (+ `frigate/test/test_file.py`); `frigate/__main__.py` (+ `frigate/test/test_validate_config_exit.py`) | Fixes cherry-picked from upstream's 0.19 `dev` that also affect the 0.18 base: a stationary `max_frames` deregister that dropped every other tracked object of the label; `skip_motion_threshold` freezing the background so motion detection never came back; the audio process crashing when a camera is added at runtime (and now restarted by the watchdog); ffmpeg output logged once on every restart, including a stalled recorder, with the heading printed by `LogPipe.dump` only when there is output; processes that looked up a camera by name crashing or leaking when a camera is added or deleted at runtime; face and classification `save_attempts` folders that only ever lost one image per save, so a folder over the limit never came back under it; `--validate-config` exiting 0 for an invalid config. The stalled-recorder dump goes through `RestartLog`, so it keeps D11's once-per-hour throttle and the SV10 summary stays. Each commit carries its upstream SHA | upstream #24418, #23994, #24246, #24398 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
