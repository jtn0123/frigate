| D54 | system: metrics history | `frigate/stats/system_history.py`, `frigate/api/system_history.py`, `frigate/test/test_system_history.py` (new); hunks in `frigate/config/telemetry.py` (`StatsHistoryConfig`), `frigate/api/fastapi_app.py` (router, sampler task), `generate_api_auth_spec.py` (the router it mounts); `web/src/hooks/fork/use-system-metrics-history.ts`, `web/src/components/fork/MetricRangeToggle.tsx` (+ `.test.tsx`), `web/src/lib/fork/metric-time-format.ts` (+ `.test.ts`), `web/e2e/specs/fork/system-metrics-range.spec.ts` (new); hunks in `web/src/views/system/GeneralMetrics.tsx` (the history source and the toggle), `web/src/components/graph/SystemGraph.tsx` (date tick labels), `web/e2e/helpers/api-mocker.ts`, `web/public/locales/en/fork.json`, `web/public/locales/en/config/global.json` and `web/src/types/fork/api.gen.ts` (generated), `docs/docs/configuration/advanced/reference.md`, `docs/static/frigate-api.yaml` (generated) | The System page's General tab could only ever graph `MAX_STATS_POINTS` (80) samples 15 s apart, about 20 minutes, held in memory by `StatsEmitter` and lost on every restart, so there was no way to see whether a detector or a GPU was worse today than yesterday. A minute sample of the graphed fields (never `cpu_usages`, which carries process command lines) is now stored in `config/system-metrics-history.sqlite` and rolled up as it ages: minutes for a day, 15 minutes for a week, 4 hours beyond that, about 2,300 rows for a full month. `GET /system/metrics/history?range=` (admin) averages a range into at most 180 points and returns them in the shape `/stats/history` uses, so the existing graphs draw them unchanged, and a range picker offers the live window, 1, 6, 12 and 24 hours, 7 and 30 days, remembered per browser; a graph whose samples span more than two days labels its ticks with dates instead of times of day. `telemetry.stats.history.enabled` (default true) and `retain_days` (default 30) turn it off or shorten it | fork-only |
