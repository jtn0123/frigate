| UI109 | UI: error state in the UI font inside the log view | `web/src/components/fork/ErrorState.tsx` (+ test), `web/e2e/specs/fork/state-polish.spec.ts` | On /logs the "Could not load this data" state inherited the log panel's monospace font and pre-wrap. ErrorState is app chrome wherever it is used, so both its layouts now set `font-sans whitespace-normal`; the server detail stays in a `code` element and the log column headers stay monospace. | fork-only |
