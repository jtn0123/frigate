| UI132 | web: one icon family in the left rail | hunks in `web/src/hooks/use-navigation.ts`, `web/src/components/menu/AccountSettings.tsx`, `web/src/components/filter/CameraGroupSelector.tsx`, `web/src/components/fork/CommandPalette.tsx` | The 52 px rail drew from five icon sets at once: Font Awesome for Live and Export, Material for Review, Classification and Ask, Ionicons for Explore, Tabler for Faces, VS Code for the account button and Material again for the "all cameras" house. Stroke weight, corner radius and optical size changed from row to row, so the column read as a pile of borrowed glyphs rather than one control surface: the filled Material camera sat two rows above a hairline Ionicons magnifier. Every rail entry is now the Lucide equivalent the design calls for (`LuVideo`, `LuGalleryThumbnails`, `LuSearch`, `LuFilm`, `LuUsersRound`, `LuShapes`, `LuMessageCircle`, `LuCircleUser`, `LuHouse`), which is the set the bell, the gear and the group icons already used. The command palette names the same destinations, so it takes the same icons. No layout, sizing or behaviour changed: each swap is one identifier, and the dev-only Playground keeps `LuConstruction`. | fork-only |
