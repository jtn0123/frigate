| UI115 | Settings: how profiles work | new `web/src/components/fork/settings/ProfilesHowItWorks.tsx`; `web/public/locales/en/fork.json`; hunk in `web/src/views/settings/ProfilesView.tsx` | With no profiles the page had an Enable toggle and a one-line description. Until a profile exists it now shows three steps (create, override per camera section, activate) and a note that only one profile is active at a time and switching applies without a restart. The activation paths listed (main menu, this page, MQTT, HTTP API, Home Assistant) come from the code. No behavior change. | fork-only |
