| UI138 | classification navigation | `web/src/views/classification/ModelSelectionView.tsx` | Save the model type selection immediately so opening a model cannot race a delayed navigation state update and return the user to the model list. | fork-only |
