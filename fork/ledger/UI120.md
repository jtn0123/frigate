| UI120 | The mark-reviewed bar keeps its count beside its button | hunk in `web/src/components/fork/review/MarkReviewedBar.tsx` | The bar's `justify-between` pushed the sentence and the button to opposite ends of a 1,700 px row, so on a wide monitor the two halves of one control sat at opposite edges of your vision. `justify-start` keeps them together; the bar's height and its position under the grid are unchanged. | fork-only |
