| F11 | deps + web: Logs page on virtua, react-logviewer removed | `web/package.json` (`@melloware/react-logviewer` and its `immutable` override removed, `virtua` ^0.49.1 direct), `web/package-lock.json`, `web/src/components/dynamic/EnhancedScrollFollow.tsx` (deleted), `web/e2e/specs/logs.spec.ts`, `FORK.md` (F6 note), `fork/PLAN2.md` (one line); hunks in `web/src/pages/Logs.tsx`, `web/src/index.css` | The log viewer is a plain virtua `VList` with stable line ids: new lines stick to the bottom only while the view is following, older ranges are prepended with `shift` so the viewport stays put, and keyboard and wheel input update the follow state. This drops react-logviewer and the `immutable` override F6 needed for it (nothing else depends on either; virtua was already installed through it and backs the timelines, so the lock only loses 5 packages and the bundle stays inside the budget). Merged onto the fork's Logs page: the camera filter (UI131), the in-page error state and retry (UI50), the filtered read that never pages (UI82), copy reading the log (UI83, kept over upstream's in-memory copy) and disabled copy/download on failure (UI116); upstream's `loadingOlderRef` replaces UI84's in-flight guard. Specs hover a log line instead of `.react-lazylog`. The range stays ^0.49.1 (upstream ^0.49.3) so the lock keeps the installed 0.49.1 | backport of upstream #24334 |
