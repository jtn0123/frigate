| I53 | Attribute verdicts in search results | `frigate/fork/event_data_keys.py`; hunks in `frigate/api/event.py` (`events_explore`, `events_search`); test in `frigate/test/http_api/test_http_fork_classification_suggestions.py` | `/events/search` and `/events/explore` copy a fixed list of `data` keys into their responses, so with a query typed in Explore the detail dialog showed an attribute-type custom model's verdict as unset even when the event had one (`/events` returns the full data, so the same event looked right without a query). The attribute-type models' keys and scores now pass through both endpoints | candidate |
