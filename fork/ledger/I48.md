| I48 | Auto-file repeat guard | `frigate/fork/classification_suggestions.py` (`auto_file_wait`), `frigate/fork/classification_prefetch.py`, `frigate/config/fork/classification_suggestions.py` (`auto_file.camera_cooldown`, `per_camera_daily_limit`); tests; `fork/CLASSIFICATION-SUGGESTIONS.md` | Auto-filing (I44) would file the owner's own car every time it came home, and upstream's docs call dozens of near-identical images the fastest way to degrade a model. The same class from the same camera now waits `camera_cooldown` seconds (2 h) between groups and files at most `per_camera_daily_limit` (10) a day. Other cameras and other classes are not held | n/a (fork-only) |
