# Copy to fork/demo/.env (gitignored). Notes only; the container does not read
# this file. First boot creates admin and prints the password in the logs.
FRIGATE_DEMO_USER=admin
FRIGATE_DEMO_PASSWORD=
# UI (TLS, self-signed): https://127.0.0.1:8971
# go2rtc (localhost only): http://127.0.0.1:1984
