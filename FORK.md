# Fork ledger

This is a UI/UX-focused fork of [blakeblackshear/frigate](https://github.com/blakeblackshear/frigate).
The backend is kept as close to upstream as possible; every divergence is listed
here with its reason and, where one exists, the upstream pull request that would
make the entry go away.

Rules that keep this fork rebasable (see the `next` branch):

- `dev` mirrors upstream and is never committed to. Pull requests land on
  `next`, which is rebased onto `upstream/dev` when upstream moves. `main` is
  the release branch and only moves with `make promote`. One report item = one
  commit.
- Add files rather than editing them. When an upstream file must change, keep
  the hunk small and self-contained.
- Fork-only UI behaviour is gated in `web/src/fork/flags.ts`.
- Never rename, move, or reformat an upstream file.
- Dependency majors upstream has not taken yet are allowed, but they come last
  (owner decision 2026-09-11): after the debugging and type-safety work, one
  PR per major (or per tightly coupled group), toolchain first. See
  `fork/PLAN2.md` PR-28.
- Commits, pushes and pull requests only ever go to `jtn0123/*` repos. Entries
  marked "candidate" could be offered upstream, but only the owner sends them;
  once one merges upstream, its ledger entry is deleted.
- Local guards: `gh repo set-default jtn0123/frigate`, `upstream` has its push
  URL set to `no_push`, and `.git/hooks/pre-push` rejects any other URL.

Every push to `main` builds `ghcr.io/jtn0123/frigate:main` (what the server
pulls) and `:<upstream base>-<date>.<run>` with
`.github/workflows/fork-build.yml`, and publishes a GitHub Release tagged
`fork/<that version>` with notes from `fork/scripts/release_notes.py`. A
build of a commit that already has a release (a re-run, say) reuses that
release's version for its tags and publishes nothing. A manually pushed
`fork/*` tag builds an image of that tag, without a release.

Daily (and on `workflow_dispatch`), `.github/workflows/fork-upstream-sync.yml`
fetches `blakeblackshear/frigate` read-only, fast-forwards `origin/dev`, and
if `next` does not already contain `upstream/dev` it rebases a copy onto
`sync/upstream` (never `next` or `main`) with `--rebase-merges`, so pull
request merges stay merges, and opens or updates an issue that lists the
conflicted files, or git's error when the rebase failed without one. A new
upstream `v*` tag after `v0.18.0-rc2` gets its own issue. Upstream's commits
change `.github/workflows`, which `GITHUB_TOKEN` may not push, so the pushes
use the `FORK_SYNC_TOKEN` secret (contents and workflows write) when it is
set, and a failed run opens an "Upstream sync failed" issue. A
`FORK_SYNC_TOKEN` push starts "Fork - Checks" on `sync/upstream` by itself;
a `GITHUB_TOKEN` push starts no workflows, so then the bot dispatches it.

## Divergences

| ID | Area | Files | Why | Upstream PR |
|----|------|-------|-----|-------------|
| S0 | scaffold | `FORK.md`, `fork/`, `web/src/fork/flags.ts`, `.github/workflows/fork-*.yml`, `.pre-commit-config.yaml`, `web/.env.example`, `web/tsconfig.e2e.json`, `Makefile` (new targets only), `.gitignore` (appended block) | Fork infrastructure: ledger, flags, CI that builds an amd64 image from this branch, inner-loop targets | n/a (fork-only) |
| G3 | backend | `frigate/api/{auth,camera,debug_replay,event,export,media,record,review}.py`, `pyproject.toml` | Route handlers no longer run peewee queries on the event loop: sync-only handlers are plain `def`, handlers that await auth wrap DB calls in `asyncio.to_thread`; full ruff `ASYNC` family enabled | candidate |
| G4 | backend | `frigate/api/event.py`, `frigate/test/http_api/test_http_event.py` | `/events/search` pushes ORDER BY and LIMIT into SQL for non-relevance sorts, bounds the candidate set to the vector-search hits, and replaces the non-sargable JSON LEFT JOIN with a windowed review-segment lookup | candidate |
| E1 | nginx | `docker/main/rootfs/usr/local/nginx/conf/security_headers.conf` (new), `nginx.conf`, `auth_request.conf`, `templates/listen.gotmpl` | Browser security headers (nosniff, SAMEORIGIN, Referrer-Policy, Permissions-Policy, report-only CSP) and HSTS re-added in every location that calls `add_header`, since nginx does not inherit them | candidate |
| E2 | backend | `frigate/api/{auth,classification,media,fastapi_app}.py`, `frigate/test/http_api/test_http_auth_gates.py`, `fork/Dockerfile.test`, `.dockerignore` | Every route declares exactly one auth gate, verified by a startup assertion and a test; bare `/review` exemption prefix replaced by exact paths; swallowed exemption exception is logged | candidate |
| E3 | backend | `frigate/api/media.py`, `frigate/test/http_api/test_http_media.py` | `preview_thumbnail` resolves the user-supplied file name with `safe_join` under `CACHE_DIR/preview_frames` instead of bare `sanitize_filename` | candidate |
| B1 | backend | `frigate/api/fastapi_app.py`, `frigate/test/http_api/test_http_password.py` | `HTTPException` handler renders `{success, message, detail}` so both API error conventions work for every client | candidate |
| B3 | backend | 15 modules under `frigate/` (see commit) | The 25 `except Exception: pass` sites log at debug with the traceback; the three bare `except:` in `frigate/util/image.py` catch `Exception` | candidate |
| G6 | backend | `frigate/record/cache_tracker.py` (new), `frigate/record/maintainer.py`, `frigate/test/test_record_cache_tracker.py`, `frigate/test/test_maintainer.py` | Recording maintainer only walks the full process table when a tracked ffmpeg writer exits or a camera has cache files with no known writer; otherwise asks just the tracked processes for open files | candidate |
| F2 | supply chain | `docker/main/Dockerfile`, `docker/main/install_deps.sh`, `docker/main/requirements-wheels.txt`, `.github/workflows/stale.yml`, `.github/dependabot.yml` | go2rtc fetched per-arch with `ADD --checksum`, ffmpeg tarballs SHA256-verified, py3nvml pinned to a commit, actions/stale pinned to v9.1.0, Dependabot covers docs, rockchip and rocm | candidate |
| F3 | deps | `docker/main/requirements-wheels.txt`, `docker/main/requirements-dev.txt`, `docker/main/requirements-ov.txt` | mypy moved out of the runtime wheel set, single `opencv-contrib-python-headless` wheel (cv2.face needs contrib), OpenVINO converter/runtime version split documented | candidate |
| H1 | docs | `AGENTS.md`, `docs/docs/development/contributing.md`, `docs/docs/integrations/api.md` (deleted) | Python version, React 19, `PROXY_HOST`, vitest command and `docker compose` syntax corrected; empty api.md that collided with the generated API route removed | candidate |
| H2 | docs | `CONTRIBUTING.md`, `README.md` | CONTRIBUTING lists every CI gate and points at the devcontainer; README links to it | candidate |
| I4 | tooling | `.pylintrc` (deleted), `.devcontainer/devcontainer.json`, `pyproject.toml` | Unused pylint config removed, dead VS Code settings dropped, ruff bandit (`S`) rules enabled with documented ignores | candidate |
| I3 | typing | `frigate/mypy.ini`, `frigate/stats/*.py`, `frigate/types.py`, `frigate/debug_replay.py` | `frigate.stats` is type-checked again (ignore block removed, 21 errors fixed, dead broken `StatsEmitter.stats_init` removed); `frigate.debug_replay` is type-checked (1 `no-untyped-def` fixed); leftover `frigate.http` ignore removed (module does not exist) | candidate |
| D1 | web tests | `web/__test__/`, `web/src/**/*.test.ts`, `web/vite.config.ts` (test block), `web/.gitignore`, `web/package.json` (`@testing-library/react`, `@testing-library/dom`), `web/src/utils/dateUtil.ts`, `web/src/lib/config-schema/transformer.ts` | Restore the vitest harness (setup file, jsdom stubs, v8 lcov coverage, unit-only include) with unit tests for the pure config, schema, date and WebSocket modules; fixes the YMD date parse and the allOf property merge those tests found | candidate |
| D5 | CI | `.github/workflows/pull_request.yml` (web_test step), `.github/workflows/fork-checks.yml` (coverage upload) | Run the unit tests in CI; the fork workflow runs them with coverage and uploads `web/coverage/lcov.info` | candidate |
| I1 | CI | `.github/workflows/pull_request.yml` (setup-node/setup-python cache, `npm ci`) | Cache npm and pip between runs and install from the lockfile so the four web jobs stop doing cold installs | candidate |
| F1 | web lint | `web/eslint.config.js` (replaces `web/.eslintrc.cjs`), `web/package.json` (eslint 9, typescript-eslint 8, jsx-a11y, globals; monaco packaging), `AGENTS.md` (one line), small hunks in `web/src` and `web/public` | ESLint 9 flat config with the old rule intent, the three config bugs fixed (duplicate ignores, prettier re-extend disabling comma-dangle, stale jest block) and jsx-a11y recommended at warn while callsites are fixed; `monaco-editor` moved to dependencies and `vite-plugin-monaco-editor` to devDependencies | candidate |
| C1 | web: error handling | `web/src/components/fork/RouteErrorBoundary.tsx`, `web/public/locales/en/fork.json`, `web/e2e/specs/fork/error-boundary.spec.ts`; hunks in `web/src/App.tsx` (wrap route Suspense and app chrome), `web/src/utils/i18n.ts` (register `fork` namespace) | Upstream has no error boundary: a render throw or a chunk-load failure after an update blanks the whole app. Flag `errorBoundary`. | candidate |
| C5 | web: error handling | `web/src/api/fork/read-error-toast.ts` (+ `.test.ts`; empty preview and `review/event/*` 404s are not toasted), `web/src/components/fork/ErrorState.tsx`, `web/e2e/specs/fork/error-state.spec.ts`; hunks in `web/src/api/index.tsx` (else branch in SWR `onError`), `web/src/components/ui/sonner.tsx` (register mounted Toasters so toasts raised before a page Toaster mounts are queued, not dropped), `web/src/pages/Events.tsx`, `web/src/pages/Exports.tsx`, `web/src/pages/System.tsx`, `web/src/views/explore/ExploreView.tsx`, `web/src/views/live/LiveDashboardView.tsx` (destructure `error`, render `ErrorState`) | Read failures were silent: no toast and either a blank area or an endless spinner. Flag `readErrorToasts` for the toast; `ErrorState` is unconditional. | candidate |
| G2 | web: data layer | hunk in `web/src/api/index.tsx` (module-scope merged axios headers, SWR `dedupingInterval`, `focusThrottleInterval`, `errorRetryCount`) | Header mutation ran on every render and clobbered other headers; refocus fired a revalidation storm across ~370 hooks and failed reads retried forever. | candidate |
| C8 | web: hygiene | hunks in `web/src/App.tsx` (DEV-only `/playground` route and lazy import, stray `{" "}` text nodes), `web/tailwind.config.cjs` (dead Next.js content globs), `web/src/context/theme-provider.tsx` (commented-out logging), `web/src/api/index.tsx` (unused `WsWithConfig` pass-through) | Production build shipped the dev sandbox page and carried small leftovers. `zoneEdutUtil.ts` and the package name are left alone because renames conflict with upstream. | candidate |
| C10 | web: typing | `web/tsconfig.fork-strict.json`, `web/scripts/fork/{typecheck-fork,type-ratchet}.mjs`, `web/eslint.ratchet.config.js`, `fork/type-ratchet.json`; hunks in `web/eslint.config.js`, `web/package.json`, `.github/workflows/fork-checks.yml`, `fork/scripts/check.sh`; fork-path type fixes | Extra TS flags and type-aware lint on fork directories only; a CI ratchet fails if hatch or type-aware-rule counts across `web/src` rise | fork-only |
| C11 | web: typing | `web/src/utils/promise.ts` (+ `.test.ts`), hunk in `web/eslint.config.js`; small hunks across `web/src` (event handlers and fire-and-forget calls) | `@typescript-eslint/no-floating-promises` and `no-misused-promises` are errors for all of `web/src` (269 findings to 0). `wrapAsync` turns async `onClick` handlers into void functions; `void` marks fire-and-forget. WebRTCPlayer now awaits the peer connection before using it (`if (!aPc)` was always true because a Promise is truthy). | candidate |
| A4 | web: shell | hunk in `web/src/App.tsx` | Config is fetched once in `App` and passed into `DefaultAppView`. The second `useSWR("config")` is gone. `WsWithConfig` was already removed in C8. Public share still skips using config. | fork-only |
| D2 | web tests | `web/e2e/specs/fork/{settings-save,camera-wizard,zone-editing,motion-search}.spec.ts`, `web/e2e/helpers/{settings-config-routes,settings-save-flow}.ts`; deleted `web/e2e/specs/settings/ui-settings.spec.ts`; hunk in `web/e2e/specs/fork/settings-nav.spec.ts` | Real e2e for the four riskiest screens: Settings Save All body and restart notice, wizard validation plus `config/set` for a new camera, zone editor empty-state Save disabled, motion search open and canvas points. Closing a Konva polygon is too brittle to assert Start Search / zone Save payloads. Settings mocks and the Save All click path are shared so Sonar new-code duplication stays under 3%. | fork-only |
| D16 | web tests | hunks in `web/e2e/helpers/api-mocker.ts` and `web/e2e/helpers/ws-mocker.ts`; drop the `stats/history` allowlist entry | Other `/api/stats/history` keys (System graphs) return a full fixture snapshot so tab clicks are not toast-blocked. Camera Health keys keep the D15 series. WS `stats` includes `processes: {}` so GeneralMetrics does not crash when history + live stats combine. | fork-only |
| A5 | web: typing | `web/scripts/fork/gen-api-types.mjs`, `web/src/types/fork/api.gen.ts`, `web/src/api/fork/client.ts` (+ `.test.ts`); hunks in fork config/review/stats readers and Live, Review, Explore, Exports, Settings, System | Frontend types are generated from `docs/static/frigate-api.yaml`. `useApi` / `apiGet` only accept spec paths and keep the existing SWR keys. `/config` and `/stats` still use the hand-written types (empty 200 schemas); `/review` stays `ReviewSegment[]` until B2 fixes date-time vs unix seconds. Renaming `ReviewSegmentResponse.id` or `EventResponse.id` and regenerating fails `tsc` at `reviewSpecId` / `eventSpecId`. | candidate |
| D9 | web: e2e | `web/e2e/scripts/validate-fixtures.mjs`, `web/scripts/fork/validate-fixtures.test.ts`; hunks in `web/e2e/global-setup.ts`, `web/e2e/playwright.config.ts`, `web/package.json` | Every JSON fixture in `web/e2e/fixtures/mock-data` is checked against the matching OpenAPI 200 schema. Unmapped files fail. `review-summary.json` is skipped (UI day-keyed shape vs spec `{ last24Hours, root }`); `config-schema.json` is skipped (editor JSON Schema, not an API list). Wired into `e2e:lint` and Playwright `globalSetup`. | fork-only |
| UI15 | web: theme | `web/src/context/fork/appearance-provider.tsx`, `web/src/components/fork/AppearanceMenu.tsx`, `web/themes/fork-appearance.css`, `web/e2e/specs/fork/appearance.spec.ts`; one-line hunks in `web/src/context/providers.tsx`, `web/src/index.css`, `web/src/components/menu/GeneralSettings.tsx` | Density, text size and OLED true-black controls persisted in localStorage and applied to `<html>`. Flag `themeControls`. | fork-only |
| T1 | web: e2e | hunk in `web/e2e/specs/config-editor.spec.ts` (press `Control+s`, not `ControlOrMeta+s`) | The desktop project spoofs a Linux user agent and Monaco derives CtrlCmd from the UA, so on a macOS host the Meta+S the old test sent was ignored and the spec failed deterministically. | candidate |
| C2 | web a11y | `web/src/utils/fork/a11y.ts`, `web/e2e/specs/fork/keyboard-a11y.spec.ts`, small hunks in ~60 components (role/tabIndex/onKeyDown, `<button>` swaps, `alt` text, sr-only DrawerTitles), `web/src/hooks/use-keyboard-listener.tsx`, `web/src/index.css` focus ring, `web/public/locales/en/*` keys | Keyboard and screen-reader access for card, menu and player click targets; shortcuts no longer fire inside textareas, selects and contenteditable regions | candidate |
| G1 | web perf | `web/src/components/icons/luIcons.ts`, `web/src/components/icons/LuIcon.tsx`, `web/src/react-icons-lu-all.d.ts`; small hunks in `IconPicker.tsx`, `CameraGroupSelector.tsx`, `ExportDialog.tsx`, `Sidebar.tsx`, `Bottombar.tsx`, `iconUtil.tsx`, `types/frigateConfig.ts`, `vite.config.ts` (alias) | The full lucide icon set (~800 kB) and the settings menus shipped in the eager chunk; now loaded on demand | candidate |
| G5 | web perf | `web/src/components/player/lazyPlayers.ts`; small hunks in `LivePlayer.tsx`, `BirdseyeLivePlayer.tsx`, `card/ReviewCard.tsx`, `card/SearchThumbnail.tsx`, `views/recording/RecordingView.tsx`, `views/search/SearchView.tsx`, `vite.config.ts` (build block) | Lazy live players (jsmpeg no longer ships to webrtc/mse users), named vendor chunks for cache stability, React.memo on the two card grids with stable callbacks | candidate |
| UI3 | web perf | `web/index.html` (one preload link) | Preload the regular Inter weight so text does not swap fonts after first paint; the rest of the first-paint work is G1 and G5 | candidate |
| UI6 | web: navigation | `web/src/components/fork/CommandPalette.tsx`, `web/src/components/fork/CommandPaletteHint.tsx`, `web/src/components/fork/ForkNavButton.tsx`, `web/src/components/fork/ForkNavItems.tsx`, `web/src/hooks/fork/use-command-palette.ts`, `web/src/lib/fork/command-items.ts`, `web/src/lib/fork/local-storage.ts`, `web/e2e/specs/fork/command-palette.spec.ts`, keys in `web/public/locales/en/fork.json`; one-line hunks in `web/src/App.tsx` (mount), `web/src/components/navigation/Sidebar.tsx` and `Bottombar.tsx` (hint button mount) | Cmd/Ctrl+K and "/" open a cmdk palette over pages, cameras (live and review), camera groups, every Settings section and quick actions (theme, mark all reviewed, restart for admins) with fuzzy filtering and recent items in localStorage. Flag `commandPalette`. | fork-only |
| UI12 | web: system | `web/src/views/fork/CameraHealthView.tsx`, `web/src/components/fork/Sparkline.tsx`, `web/src/hooks/fork/use-stats-history.ts`, `web/src/lib/fork/camera-health.ts`, `web/e2e/specs/fork/camera-health.spec.ts`, keys in `web/public/locales/en/fork.json`; small hunks in `web/src/pages/System.tsx` (new `health` tab) and `web/public/locales/en/views/system.json` (tab title keys) | "Health" tab on the System page with one card per camera: camera, detection and skipped fps, detector share, reconnects and stalls, the 0.18 connection quality indicator, an inline SVG fps sparkline from successive stats updates, an ok / degraded / offline / disabled state with reasons, and links to live view and camera settings. Flag `cameraHealth`. | fork-only |
| UI14 | web: notifications | `web/src/components/fork/InboxBell.tsx`, `web/src/hooks/fork/use-inbox.ts`, `web/src/lib/fork/inbox-store.ts`, `web/e2e/specs/fork/notification-inbox.spec.ts`, keys in `web/public/locales/en/fork.json`; mounted through `ForkNavItems` (no further upstream hunks) | In-app inbox fed by the `reviews` WebSocket topic: capped localStorage list with unread badge on a bell in the sidebar / bottombar, panel with thumbnail, camera, labels, time and review deep link, mark read / dismiss / clear, per-camera mute and quiet hours. No browser push. Flag `notificationInbox`. | fork-only |
| E4 | security | CodeQL alerts on `jtn0123/frigate` (triaged in GitHub; no fork code changes) | First CodeQL scan findings were all in upstream code or disabled workflows: critical command-line / SSRF sinks are admin-only routes with argv-list spawns or host validation; clear-text-logging alerts are false positives (env var names, redacted URLs, or first-boot console password); polynomial-redos needs an admin-controlled token; disabled-workflow permission alerts dismissed as won't-fix | candidate |
| E5 | deps | `docker/main/requirements-wheels.txt`, `web/package.json`, `web/package-lock.json` | Patch shipped advisories: `python-multipart` 0.0.26→0.0.31; web axios/react-router-dom/postcss/vitest 3.x and overrides for nanoid/fast-uri/browserslist/form-data; `i18next-cli` pinned to 1.5.11 so extract:ci stays stable; close Dependabot #1–#4. 2026-09-11: brace-expansion 1.1.11→1.1.18 (Dependabot #9) and an `i18next-cli` → `glob` 11.1.0 override (the alert behind Dependabot #8, keeping the pin); sort-by/object-path went with F5. Left open: react-router 7 and vitest 4+ majors | candidate |
| F5 | deps | `web/package.json`, `web/package-lock.json` | Retire dormant dependencies: the unused `sort-by` removed (cleared three object-path alerts); `strftime`, `nosleep.js`, the monaco plugin hack and `web/patches/README.md` still to do | candidate |
| F6 | deps | `web/package-lock.json` only | Minor/patch refresh within current majors (59 direct deps, 2026-09-11); `package.json` untouched. Follow-up the same day (owner OK): konva 10.5 and monaco-yaml 5.5 with public-API hunks in `web/src/components/settings/PolygonCanvas.tsx` (`getAbsolutePosition()` for the private `_lastPos`) and `web/src/pages/ConfigEditor.tsx` (`format: { enable: true }`); react-logviewer 6.5.5 with an `immutable` 5.1.9 override (its virtua 0.49 drops `findStartIndex`/`findEndIndex`, so `web/src/pages/Logs.tsx` reads `findItemIndex` over the scroll window; history case in `web/e2e/specs/logs.spec.ts`); @playwright/test 1.63; prettier 3.9 with the 36 files it re-wraps reformatted (65+/127-, 34 upstream). Still held: react-apexcharts 1.4 (needs the apexcharts major) | candidate |
| I5 | web: env | `web/src/vite-env.d.ts`, `web/.env.example` | Declare `VITE_GIT_COMMIT_HASH` on `ImportMetaEnv` and document `E2E_PORT` for parallel worktrees | fork-only |
| G7 | web: perf | `web/scripts/fork/bundle-budget.mjs`, `fork/bundle-budget.json`, `web/package.json`, `.github/workflows/fork-checks.yml` | Fail CI when eager JS+CSS gzip exceeds the measured size plus 5% | fork-only |
| I6 | CI | `.github/workflows/fork-upstream-sync.yml`, `.github/workflows/fork-checks.yml` | Daily read-only sync of upstream `dev` onto `origin/dev`; rebase preview on `sync/upstream`; issues for drift and new `v*` tags | fork-only |
| I12 | CI | `.github/workflows/fork-upstream-sync.yml` | Drop markdown backticks in the issue-body printf so actionlint SC2016 is clean | n/a (fork-only)
| D8 | CI | `.github/workflows/fork-checks.yml`, `docker/main/requirements-dev.txt` | Backend tests in `frigate-fork-test` run under `coverage`; the job prints a summary and uploads XML. No fail gate. | candidate |
| D10 | backend: camera watchdog | `frigate/video/hwaccel_fallback.py` (+ `frigate/test/test_hwaccel_fallback.py`); hunks in `frigate/video/ffmpeg.py` (`CameraWatchdog`: note hwaccel crashes, restart detect with the software command, reset on ffmpeg config change), `frigate/camera/__init__.py` (`hwaccel_fallback` metric), `frigate/stats/util.py` (per-camera `hwaccel_fallback`); web: `web/src/lib/fork/camera-health.ts` (+ `.test.ts`, `softwareDecoding` reason), `web/src/types/stats.ts`, hunk in `web/src/hooks/use-stats.ts` (status-bar warning), keys in `web/public/locales/en/fork.json`, a case in `web/e2e/specs/fork/camera-health.spec.ts` | When hardware decoding ends a camera's detect ffmpeg 3 times in 10 minutes (VAAPI "Failed to sync surface" crashed one Tapo C120 every ~40 s on a UHD 730), that camera's detect stream restarts with software decoding, one warning is logged and the UI flags it, instead of the stream being killed and restarted forever | candidate |
| D11 | backend: camera watchdog | `frigate/video/restart_log.py` (+ `frigate/test/test_restart_log.py`); hunks in `frigate/video/ffmpeg.py` (`reset_capture_thread` and the other-process restart go through `RestartLog`; the stale-record restart is recorded; `cause` for watchdog-initiated restarts; "crashed unexpectedly" logged once per crash instead of every second until the retry), `frigate/camera/__init__.py` (`restart_events` list), `frigate/stats/util.py` (per-camera `restarts_24h`, `restart_kinds_24h`, `recent_restarts`); web: collapsed restart line in `web/src/views/fork/CameraHealthView.tsx`, helpers in `web/src/lib/fork/camera-health.ts`, types in `web/src/types/stats.ts`, keys in `web/public/locales/en/fork.json`, a case in `web/e2e/specs/fork/camera-health.spec.ts` | Every ffmpeg restart is kept for 24 h with a reason (hardware decoding, connection, stalled, other) and shown on Camera Health as one collapsed line; the full 100-line ffmpeg dump is logged once per failure kind per hour and repeats get one line with a count, so a camera in a bad spell no longer floods the log | candidate |
| D12 | backend: camera watchdog | `frigate/video/restart_log.py`, `frigate/test/test_restart_log.py` | Repeat "exited again" warnings doubled the period when ffmpeg's message already ended in "." | candidate |
| D13 | web: build | `web/src/components/ui/circular-progress-bar.tsx` (+ `.test.tsx`) | Tailwind warned that `delay-[var(--delay)]` and `duration-[var(--transition-length)]` were ambiguous; those timings are inline styles now | candidate |
| D14 | backend + web: camera health | `frigate/video/hwaccel_fallback.py` (the switch is kept in `/config/.fork/hwaccel_fallback/<camera>.json` with a hash of the configured command, + cases in `frigate/test/test_hwaccel_fallback.py`); hunks in `frigate/video/ffmpeg.py` (`CameraWatchdog` restores, logs and publishes the switch), `frigate/camera/__init__.py` + `frigate/stats/util.py` (per-camera `hwaccel_fallback_since`); web: `web/src/lib/fork/camera-health.ts` (+ `.test.ts`), `web/src/views/fork/CameraHealthView.tsx`, hunk in `web/src/hooks/use-stats.ts`, `web/src/types/stats.ts`, keys in `web/public/locales/en/fork.json`, cases in `web/e2e/specs/fork/camera-health.spec.ts` | After an update 7 of 9 cards said Degraded while every camera ran at full rate: any reconnect, stall or skipped frame in the last hour counted, and the software-decoding switch was forgotten on restart, so the flaky cameras crashed three more times first. Now Degraded means lasting trouble, the first 2 minutes show Starting, software decoding is a note (status bar for a day), and the switch is kept for 7 days across restarts unless the camera's ffmpeg settings change | candidate |
| D15 | web: camera health | `web/src/components/fork/Sparkline.tsx` (scaled from zero, dashed target line, time-spaced points, no one-point dot), `web/src/hooks/fork/use-stats-history.ts` (seeded from `/stats/history`, then live stats), helpers in `web/src/lib/fork/camera-health.ts` (+ `.test.ts`), `web/src/views/fork/CameraHealthView.tsx`, keys in `web/public/locales/en/fork.json`; e2e: a `/api/stats/history` mock for the chart's keys in `web/e2e/helpers/api-mocker.ts` (other keys are a full fixture snapshot; D16), a case in `web/e2e/specs/fork/camera-health.spec.ts` | Each card's frame-rate chart was a one-point dash on every visit (it only collected the once-a-minute live stats while the page was open) and, once filled, a flat line pressed against the top edge with no scale, so it read as a divider. It now opens with the server's last ~20 minutes and sits at its level under a dashed target line with a caption and legend; the per-card "Updated" line (the header has one) and the extra state dot are gone, and charts and buttons line up across a row | n/a (fork-only) |
| C12 | web: static analysis | fork-added web files: `Readonly` props across `web/src/components/fork/**`, `web/src/views/fork/CameraHealthView.tsx`, `web/src/context/fork/appearance-provider.tsx`, `web/src/components/icons/LuIcon.tsx`; required-with-`undefined` fields in `web/src/lib/fork/{event-summary,inbox-store,settings-diff}.ts`; `web/src/components/fork/{CommandPalette,ShareClipButton,RouteErrorBoundary}.tsx`, `web/src/components/fork/settings/{SettingsNav,SettingsReviewDialog}.tsx`, `web/src/components/icons/luIcons.ts`, `web/src/utils/promise.ts`, `web/__test__/test-setup.ts`, two unit tests; the vendored `web/src/lib/fork/qr-encode.ts` lint exemption moved into the ignores of `web/eslint.config.js` and `web/eslint.ratchet.config.js`; `fork/type-ratchet.json` tightened for the lower counts | SonarCloud reported 46 issues in fork-added web files; 45 are fixed without behaviour change, and the appearance menu's focus sentinel is kept on purpose | n/a (fork-only) |
| D19 (committed as D16) | web tests | `web/e2e/playwright.config.ts` (one `grepInvert` per project), 11 specs in `web/e2e/specs/fork/` | Layout-only fork tests carry `@desktop-only` / `@mobile-only` tags and are not collected on the other project, instead of 29 run-time `test.skip(isMobile)` calls that Sonar flags (S1607) and that showed up as skipped in every run | n/a (fork-only) |
| I14 | CI + scripts: static analysis | `.github/actions/fork-web-setup/action.yml` (`npm ci --ignore-scripts` + `postinstall`), `.github/workflows/fork-checks.yml` (local binaries instead of `npx`, `curl --proto '=https'`, hash-locked Python install), `.github/workflows/fork-build.yml` (action pinned to a commit SHA), `fork/Dockerfile.test` + `fork/requirements-dev.lock` (new) + `fork/scripts/dev-lock-check.py` (new; fails the image build when `requirements-dev.txt` drifts from the lock), `fork/scripts/{check,ci-changes,py-checks,wt}.sh`, `fork/demo/fetch-samples.sh`, `web/scripts/fork/{bundle-budget,type-ratchet}.mjs` (+ `type-ratchet.test.ts`), `frigate/api/fork_share.py`, `frigate/record/cache_tracker.py`, `frigate/test/http_api/test_http_auth_gates.py`, `migrations/036_create_share_link.py` (rollback drops the table) + `frigate/test/test_migration_share_link.py` (new) | SonarCloud reported about 40 findings in the fork's CI, scripts, test image and backend files: on-demand `npx` installs, npm lifecycle scripts, unlocked pip installs, a tag-pinned action, HTTP redirects allowed, a backtracking regex (6.3 s on 100k spaces, now 0 ms), shell style and an empty migration rollback. All are fixed; the only behaviour changes are the stricter installs and a rollback that works | n/a (fork-only) |
| I9 | CI | `.github/workflows/fork-checks.yml`, `.github/workflows/fork-build.yml` (paths-ignore), `.github/actions/fork-web-setup/`, `fork/scripts/ci-changes.sh`, `fork/scripts/py-checks.sh`, `fork/Dockerfile.test`, `web/package.json` (`typecheck` script; `lint` caches) | Faster "Fork - Checks": docs-only commits skip the web and Python suites, node_modules is cached, one typecheck instead of three, the e2e bundle is built once and Playwright runs in three shards, mypy/API spec/unittest run in parallel, superseded runs are cancelled, "Checks passed" is the one job to require, and the image build skips pushes that cannot change the image | n/a (fork-only) |
| I10 | tooling | `Makefile` (fork block only), `fork/scripts/check.sh`, `fork/scripts/wt.sh`, `fork/README.md`, `.pre-commit-config.yaml` (eslint cache), `.gitignore` (appended), `fork/PLAN.md` | Local loop: `make check` / `check-fast` run the CI gates (host and Docker lanes), incremental tsc and cached eslint, the pinned ruff through uvx, one test image per worktree, and `make wt` for ready-to-use worktrees | n/a (fork-only) |
| UI7 | web: settings | `web/src/components/fork/settings/SettingsNav.tsx`, `web/src/components/fork/settings/SettingsReviewDialog.tsx`, `web/src/hooks/fork/use-settings-nav.ts`, `web/src/lib/fork/settings-diff.ts`, `web/e2e/specs/fork/settings-nav.spec.ts`, keys in `web/public/locales/en/fork.json`; small hunks in `web/src/pages/Settings.tsx` (publish + Save All intercept + mount) and `web/src/components/config-form/theme/templates/ObjectFieldTemplate.tsx` (`data-settings-anchor`) | Scrollspy section rail, settings search (sections and in-page field labels), and a before/after diff dialog ahead of Save All. Flag `settingsNav`. | fork-only |
| I7 | tooling | `fork/demo/`, `Makefile` (`demo-up` / `demo-down` / `demo-logs`) | Local overlay demo on `ghcr.io/blakeblackshear/frigate:0.18.0-rc2` with looping sample cameras, auth on, ports on `127.0.0.1` only. Named volumes instead of USB bind-mounts. | n/a (fork-only) |
| UI10 | web: explore/review | `web/src/components/fork/bulk/BulkActionBar.tsx`, `web/src/hooks/fork/use-bulk-selection.ts`, `web/src/lib/fork/bulk-actions.tsx`, `web/e2e/specs/fork/bulk-actions.spec.ts`, keys in `web/public/locales/en/fork.json`; small hunks in `web/src/views/search/SearchView.tsx` (select mode), `web/src/components/filter/ReviewActionGroup.tsx` and `web/src/pages/Events.tsx` (undo toast) | Multi-select in Explore (range, select-all, delete, Frigate+) and an undo toast for Review mark-reviewed. Flag `bulkActions`. | fork-only |
| UI8 | web: timeline | `web/src/lib/fork/timeline-scrubber.ts`, `web/e2e/specs/fork/timeline-scrubber.spec.ts`, keys in `web/public/locales/en/fork.json`; small hunks in `web/src/components/timeline/{ReviewTimeline,EventReviewTimeline,MotionReviewTimeline}.tsx` | Snap-to-event on handlebar release, Left/Right stepping between events, and larger touch targets. Flag `timelineScrubber`. | fork-only |
| UI9 | web: explore/review | `web/src/components/fork/EventSummaryHeader.tsx`, `web/src/lib/fork/event-summary.ts`, `web/e2e/specs/fork/event-summary.spec.ts`; small hunks in `SearchDetailDialog.tsx` and `DetailStream.tsx` | Shared camera / label / time / duration / zone row on Explore and Review detail panels. Flag `unifiedEventDetail`. | fork-only |
| I13 | CI + tooling | `fork/scripts/release_notes.py` (+ `test_release_notes.py`), `fork/scripts/promote.sh`, `.github/workflows/fork-build.yml` (version tag, `release` job), `Makefile` (`promote`, `fork/scripts` in lint/format), `fork/scripts/check.sh` (`scripts` gate), `.github/workflows/fork-checks.yml` + `fork/scripts/ci-changes.sh` (lint and test `fork/scripts`) | Every `main` build is also tagged `:<upstream base>-<date>.<run>` and gets a GitHub Release whose notes come from the fork's own commits: grouped by ledger ID, internal work counted instead of listed, tooling lines folded under "Under the hood", `Release-note:` trailers to override, rebase-proof, ending in a build marker the app matches. `make promote` moves `main` to `next` after Fork - Checks is green | n/a (fork-only) |
| UI42 | web+backend: updates | `frigate/fork/updates.py`, `frigate/api/fork_updates.py` (+ `frigate/test/test_fork_updates.py`, `frigate/test/http_api/test_http_fork_updates.py`), `web/src/lib/fork/updates.ts` (+ `.test.ts`), `web/src/hooks/fork/use-fork-updates.ts`, `web/src/components/fork/updates/{UpdateNotices,ReleaseNotesDialog}.tsx`, `web/e2e/specs/fork/update-notices.spec.ts`, `web/e2e/fixtures/mock-data/fork-updates.ts`, keys in `web/public/locales/en/fork.json`; hunks in `fastapi_app.py` (router), `auth.py` (admin-default skip), `frigate/stats/util.py` (`latest_version` from the fork's releases, never upstream), `ForkNavItems.tsx`, `CommandPalette.tsx` (What's new action), `web/e2e/helpers/api-mocker.ts` (default mock), `docs/static/frigate-api.yaml` | `GET /api/fork/updates` compares the running build (the SHA `make version` stamps) with `jtn0123/frigate` releases, cached 6 h and honoring `telemetry.version_check`. Admins get an update button with the notes and how to pull; everyone gets What's new once per release this browser has not seen, and from the command palette. Flag `updateNotices`. | fork-only |
| UI11 | web+backend: share | `frigate/api/fork_share.py`, `ShareLink` + `migrations/036_create_share_link.py`, `web/src/lib/fork/qr.ts`, `web/src/lib/fork/share-path.ts`, `web/src/components/fork/ShareClipButton.tsx`, `web/src/pages/fork/ShareClipPage.tsx`, `web/e2e/specs/fork/clip-sharing.spec.ts`; hunks in `auth.py` (admin-default skip), `nginx.conf` (GET auth_request off), `App.tsx`, `SearchDetailDialog.tsx`, `DetailStream.tsx` | Expiring clip share link with a QR code. Public GET uses `allow_public`; token is the credential. Flag `clipSharing`. | fork-only |
| D17 | backend: audio transcription | `frigate/embeddings/transcription_preload.py` (+ `frigate/test/test_transcription_preload.py`); hunk in `frigate/embeddings/__init__.py` (`EmbeddingProcess.run` preloads first) | On the `-rocm` image, turning on audio transcription crash-looped the embeddings process every ~20 s ("free(): invalid pointer"): faster-whisper loaded ctranslate2 after onnxruntime's MIGraphX provider, which aborts, and semantic search and face recognition went down with it. ctranslate2 is now imported before any onnxruntime session when a camera transcribes | candidate |
| I15 (first committed on `main` as I13) | CI | `.github/workflows/fork-build.yml` (`rocm` job; both image jobs clear the runner's tool cache after setup), hunk in `.github/actions/setup/action.yml` (`root-reserve-mb: 8192`, so the runner's logs cannot fill the root disk; the default 1 GB crashed both image jobs within a minute) | Every build also publishes a ROCm image for AMD GPUs from upstream's `docker/rocm/rocm.hcl` (ONNX detector on ROCm, VAAPI decode through radeonsi), tagged like amd64 with a `-rocm` suffix: `:<sha>-rocm`, `:<version>-rocm`, and `:main-rocm` from `main` only. Layers come from the fork's and upstream's public rocm and amd64 caches. Replaces the `rocm` job pushed straight to `main` as "I13" in e14acb197, before that ID was taken on `next` | n/a (fork-only) |
| D18 | backend: GenAI (Ollama) | hunk in `frigate/genai/plugins/ollama.py` (`estimate_image_tokens` probe and per-size cache), `frigate/test/test_ollama_image_tokens.py` | Review summaries sized their frame count with the generic 1 token per 1,250 px guess, but qwen3-vl on Ollama spends ~1,055 tokens per 180p preview frame (the guess says 46), so long alerts sent ~55 frames and Ollama rejected the 58k-token requests. The Ollama client now measures the real per-image cost once per size, like the llama.cpp client, reusing the configured num_ctx and keep_alive so the probe never reloads the model | candidate |
| D21 | backend + web: upstream 0.19 backports | `frigate/output/birdseye.py` (+ `frigate/test/test_birdseye.py`); `frigate/video/ffmpeg.py` (`capture_frames`); `frigate/api/media.py` (`_run_clip_download`, `event_thumbnail`), `frigate/util/ffmpeg.py` (`terminate_ffmpeg_stream`), `frigate/jobs/motion_search_decode.py` (+ `frigate/test/test_clip_download.py`); `frigate/util/builtin.py` (`load_labels` prefill), `frigate/object_detection/base.py`, `frigate/util/services.py` (keyframe gap pattern), `KeyframeAnalysisSection.tsx`, `web/src/types/stats.ts`, keys in `web/public/locales/en/views/system.json`; `frigate/util/file.py` (+ `frigate/test/test_file.py`); `docker/main/build_nginx.sh`, `nginx.conf`, `GenericVideoPlayer.tsx`; `frigate/api/record.py`, `generate_api_auth_spec.py`, `docs/static/frigate-api.yaml` (+ `frigate/test/http_api/test_http_media.py`); `PreviewPlayer.tsx`, `PreviewThumbnailPlayer.tsx`, `use-camera-previews.ts`, `use-hour-rollover.ts` (new), `dateUtil.ts`, `Events.tsx`, `MotionSearchView.tsx` (+ `web/e2e/specs/preview-rollover.spec.ts`); `ObjectDetectProcess.start_or_restart` (+ `frigate/test/test_detector_restart.py`) | Nine fixes cherry-picked from upstream's 0.19 `dev` that also affect the 0.18 base, so they ship now instead of waiting for the 0.19 rebase: birdseye cameras overlapping with mixed portrait and landscape feeds; a config subscriber left behind by every capture thread restart; clip downloads that deadlock, then hang every sync endpoint, when ffmpeg writes over 64 KB of warnings; a phantom `unknown` object type from labelmap padding; a 500 on an empty thumbnail file; nginx-vod on the maintained dio-az fork, so a real 5xx is no longer reported as "no recordings"; recordings API default bounds frozen at process start; preview tiles going blank after the hour rolls over; a recovered detector killed during restart. Each commit carries its upstream SHA (`cherry-pick -x`). Conflicts were resolved toward the fork (G3 sync handlers, the Sonar `download_https` helper) without taking 0.19-only code | upstream #22917, #24002, #24032, #24067, #24110, #24123, #24121, #24157, #24146 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
| E7 | security: config substitution | `frigate/config/env.py` (sources merged by precedence, `secrets.yaml`, `UnknownVariableError`), `frigate/util/services.py` (`GO2RTC_ALLOW_ARBITRARY_EXEC` read once at import), `frigate/config/config.py` (`reload_sources` on parse), `docker/main/rootfs/usr/local/go2rtc/create_config.py`, `frigate/api/camera.py`, `docs/docs/configuration/{advanced/system,config,restream}.md`, `docs/docs/frigate/installation.md` (+ `frigate/test/test_env.py`, `frigate/test/http_api/test_http_camera_access.py`) | The config's `environment_vars` block is exported into the process environment, and the go2rtc exec gate read `GO2RTC_ALLOW_ARBITRARY_EXEC` from it live, so anyone with config write access could turn on `exec:` stream sources and run commands in the container. The gate now reads the variable once at import, before any config loads. Upstream's change also brings a `secrets.yaml` source, one precedence order shared with go2rtc (credentials directory, then container environment, then `secrets.yaml`, then `environment_vars`), and an undefined `{FRIGATE_*}` name reported as a config validation error | upstream #24044 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
| D20 | web tests: e2e fixtures | `web/e2e/fixtures/mock-data/generate-mock-data.py` and its output `{reviews,review-summary,events,exports}.json`; `web/__test__/e2e-fixture-contract.test.ts`; new cases in `web/e2e/specs/{review,explore}.spec.ts` | The fixtures did not match the API, so the e2e suite ran against data the UI cannot render and still passed: review times were ISO strings (the model types them as datetime, but `/review` returns epoch seconds), so every card read "Invalid Time"; review and export thumb paths lacked `/media/frigate/`, which the UI strips, giving broken `//clips` URLs; the summary had no `last24Hours`, so the severity badges read 0; and events had no `event_count`, so Explore headers showed the bare key "Trackedobjectscount". Found by a dogfood pass. The unit test checks the fixture contract; the specs check the rendered result. D9's fixture validator lists the review times in `EPOCH_FIELD_DRIFT` (`web/e2e/scripts/validate-fixtures.mjs`): the spec types them as date-time from `ReviewSegmentResponse`, but `/review` returns the rows through `JSONResponse`, so the fixture must hold epoch seconds and the rest of each review is still checked against the spec | n/a (fork-only) |
| UI43 | web: config editor | hunks in `web/src/pages/ConfigEditor.tsx` (Save & Restart asks first, then saves, then restarts; translated fallback for the save toast) and `web/src/components/overlay/dialog/RestartDialog.tsx` (`onRestart` may resolve `false` to cancel before the restarting sheet opens); key `configEditor.saved` in `web/public/locales/en/fork.json`; cases in `web/e2e/specs/config-editor.spec.ts` | Save & Restart wrote the config before showing "Are you sure you want to restart Frigate?", so Cancel skipped only the restart and left the new config on disk with nothing to say so. Now nothing is saved until the restart is confirmed, and a failed save closes the dialog, shows the error, and never restarts. The save toast showed an empty bar when the response had no `message` | n/a (fork-only) |
| UI45 | web: system | hunk in `web/src/pages/System.tsx`; case in `web/e2e/specs/system.spec.ts` | `useHashState` yields `""` at a bare `/system`, and `page ?? "general"` kept it, so no tab was selected or marked visited and the page body stayed empty. Typed URLs, bookmarks and links from outside the app hit it; in-app links add `#general` | n/a (fork-only) |
| UI44 | web: settings | hunk in `web/src/views/settings/UiSettingsView.tsx` (`SwitchSettingRow`); `web/e2e/specs/settings/ui-settings.spec.ts` | Each Live Dashboard switch renders twice (beside the label on phones, in the control column from md up) and the label's `htmlFor` pointed at the phone copy, hidden on desktop. Clicking a setting's title did nothing and the visible switch had no accessible name. The title now toggles the setting itself and names both switches through `aria-labelledby` | n/a (fork-only) |
| UI46 | web: live | hunks in `web/src/components/filter/CameraGroupSelector.tsx` (a `GroupIcon` fallback, group names as button labels); case in `web/e2e/specs/live.spec.ts` | The backend's default group icon is `generic`, which is not a Lucide name, so `LuIcon` rendered nothing and those groups showed as blank sidebar buttons. Every group button was also labelled "Camera Groups". Unknown icons now fall back to a grid glyph and each button is named after its group | n/a (fork-only) |
| UI48 | web: recordings | hunk in `web/src/components/player/dynamic/DynamicVideoPlayer.tsx`; case in `web/e2e/specs/review.spec.ts` | While a recording loaded and there was no preview for the range, the preview player's "No Preview Found" rendered under the loading spinner, centered on the same spot. The preview player now stays hidden during a load unless there is a preview to show; "a preview" mirrors `PreviewPlayer` (a preview file, or frames for the current or previous hour since D21's rollover fix) | n/a (fork-only) |
| UI49 | web: exports | hunks in `web/src/components/card/ExportCard.tsx` (`onError` on the case and export thumbnails); case in `web/e2e/specs/export.spec.ts` | A thumbnail that failed to load (pruned or deleted file) left the browser's broken-image glyph on the card, with its alt text across the top of export cards, and the export card's skeleton only cleared on load. Failed thumbnails now fall back to the card's plain background and clear the skeleton | n/a (fork-only) |
| UI52 | web: wording | `web/src/lib/fork/{detector-name,platform}.ts` (+ `.test.ts`); hunks in `web/src/hooks/use-stats.ts` (detector name in the slow-detector warnings) and `web/src/components/fork/{CommandPalette,CommandPaletteHint}.tsx`; key `commandPalette.footerHintMac` in `web/public/locales/en/fork.json`; a status bar case in `web/e2e/specs/live.spec.ts` | Three small wording fixes from a dogfood pass: the slow-detector warning capitalized only the first letter of the detector key, so the common `cpu` detector read "Cpu is slow" (hardware acronyms are now upper-cased); the palette footer said "Ctrl+K" on Macs, where the shortcut hint is "⌘K"; and the command palette button used the same magnifying glass as the Explore nav item, so the two read as one control. It now uses a command glyph | n/a (fork-only) |
| UI47 | web: error toasts | hunk in `web/src/components/ui/sonner.tsx` (error toast description color); `web/src/api/fork/read-error-toast.ts` (one shared toast id) (+ case in `.test.ts`); case in `web/e2e/specs/fork/error-state.spec.ts` | Error toasts sit on `bg-danger`, and their description used the muted gray meant for neutral toasts, about 1.4:1 against the red, so the detail line was unreadable. It now uses the toast's foreground (at least 3:1, checked by the spec). Read-error toasts (C5) were keyed per endpoint, so a page that failed several reads at once, such as opening a recording, stacked one toast per endpoint over its toolbar; they now share one id and the newest replaces the rest, with the per-endpoint cooldown unchanged | n/a (fork-only) |
| UI51 | web: explore | hunk in `web/src/views/search/SearchView.tsx` (filter bar `justify-start`); case in `web/e2e/specs/explore.spec.ts` | The Explore filter bar scrolls horizontally but was `justify-end`, so when it overflowed a phone screen its first button ("All Cameras") sat off the left edge, where a scroll container cannot reach. The bar is `w-max`, so on wider screens there is no free space and the alignment change is invisible | n/a (fork-only) |
| C13 | web: accessibility | hunks in `web/src/components/menu/{GeneralSettings,AccountSettings}.tsx`, `web/src/components/navigation/Sidebar.tsx`, `web/src/components/overlay/detail/{DetailActionsMenu,SearchDetailDialog}.tsx`, `web/src/pages/System.tsx`, `web/src/components/card/ExportCard.tsx`; keys `a11y.{home,moreActions}` in `web/public/locales/en/fork.json`; `web/e2e/specs/fork/a11y-names.spec.ts`; the delete-race case in `web/e2e/specs/export.spec.ts` follows the renamed kebab, and `web/e2e/specs/{ai-models,model-health-followup,navigation}.spec.ts` follow the translated System tab names and the named Settings trigger | A dogfood pass found unnamed and nested controls: the Settings and Account triggers had no name (Account's was a `div`), the logo link had no name, the Explore detail menu nested a `div role="button"` in its trigger, the sub label pencil and Top Score info were a clickable svg and div, tab names used raw ids ("Select tracking_details"), and the export card kebab nested a named element inside its trigger under the wrong name "Edit name". Each is now one named control. The sidebar's translation hook does not suspend, since suspending held back the lazy Settings and Account menus | n/a (fork-only) |
| UI50 | web: logs | hunks in `web/src/pages/Logs.tsx`; cases in `web/e2e/specs/logs.spec.ts` (the websocket-tab case follows the tab's new name) | A failed or malformed initial load (for example a proxy's HTML page) left an empty log table, and stream failures toasted the raw JavaScript error ("Cannot read properties of undefined"). The table now shows the fork `ErrorState` with Retry, streaming starts only after a good initial load, and stream failures toast the HTTP status or a translated fallback. On phones the source tabs ran under the Copy/Download/Filter buttons, so tapping "Messages" hit Copy; the tab strip now shrinks and scrolls beside them. Tab names were raw ids ("Select websocket") and title-cased the product names into "Go2rtc" and "Nginx" | n/a (fork-only) |
| D22 | web tests | `web/e2e/specs/fork/android-phone.spec.ts`; hunk in `web/e2e/playwright.config.ts` (mobile project) | The phones this fork targets are Android flagships in Chrome, but the mobile project sent an iPhone user agent from Chromium at 390x844, so every `isIOS` branch ran and the Android paths (PiP button, `contextmenu` long-press, element fullscreen) never did. It now runs as a Galaxy S24 Ultra (Android 14 Chrome UA, 412x915 at 3.5x, touch), and the new spec covers the phone behaviour below | fork-only |
| UI53 | web: phones | `web/src/lib/fork/overlay-history.ts` (+ `.test.ts`), `web/src/lib/fork/phone.ts`, `web/src/hooks/fork/use-overlay-history-back.ts`; hunks in `web/src/hooks/use-history-back.ts` (export switch), `web/src/components/ui/{dialog,sheet}.tsx` (default), `web/src/components/ui/{drawer,alert-dialog}.tsx` (history-aware root) | Android's back gesture left the page with a menu open: vaul drawers had no history handling and dialogs, sheets and alert dialogs defaulted it off. On phones they now all close on back, one per press: a single `popstate` listener closes the top-most overlay, and the `history.back()` an overlay makes when it closes itself is not read as a back press by the others (upstream's per-overlay listeners closed every open overlay at once). Each overlay entry copies the router's `history.state` and an overlay only steps back while the current entry is still its own, so a view that stores state in history (the recording view's timeline mode, picked from a drawer) is not undone. Popovers (info bubbles) get the same history-aware root on phones (hunk in `web/src/components/ui/popover.tsx`), so back closes them instead of leaving the page. Dropdown menus are left as upstream: `dropdown-menu.tsx` is not Prettier-formatted upstream, so any edit would reformat the whole file. Flag `phoneFixes` | candidate |
| UI54 | web: phones | `web/src/lib/fork/phone-shell.ts`; hunks in `web/src/App.tsx`, `web/src/components/navigation/Bottombar.tsx`, `web/src/components/mobile/MobilePage.tsx` | Gesture-bar and cutout insets were reserved only in the installed PWA, so in an edge-to-edge Chrome tab the bottom bar could sit under Android's gesture handle; and a phone in landscape (915 px wide) crossed `md` and got the tablet's 4rem bar on a 412 px tall screen. Insets now apply in a tab too (zero where Chrome draws no system UI) and phones keep the 3rem bar in landscape. The single-camera settings cog, which in landscape kept its `p-2` inside a `size-9` box plus the `md` icon margin and shrank to about 8 px, drops the padding on phones (hunk in `web/src/views/live/LiveCameraView.tsx`). Flag `phoneFixes` | candidate |
| UI55 | web: phones | `web/src/lib/fork/fullscreen.ts`, `web/src/lib/fork/phone-fullscreen.ts`, `web/src/hooks/fork/use-fullscreen-orientation.ts`; hunks in `web/src/components/ui/drawer.tsx` (`portalContainer`), `web/src/views/live/LiveCameraView.tsx`, `web/src/views/live/LiveDashboardView.tsx`, `web/src/views/recording/RecordingView.tsx` | Fullscreen on phones: the camera settings drawer (detect, record, audio) is rendered inside the fullscreen element instead of hidden, and the controls, which were a grey gradient back button, toggles in two sizes and a round settings button stacked in a corner, become one frosted rail of 40 px buttons (inactive translucent, active blue) centered on the left edge in landscape and along the bottom in portrait. The live grid's fullscreen button sits in the Live header beside the grid/list toggles instead of floating over the tiles. Recording playback locks to the orientation that fits the camera, as single-camera live already did. Flag `phoneFixes` | candidate |
| UI56 | web: phones | `web/src/components/fork/PhoneDetailHeader.tsx`; hunks in `web/src/components/fork/CommandPalette.tsx`, `web/src/components/overlay/detail/SearchDetailDialog.tsx` | On phones Explore's full-screen detail set the title beside the event summary, so "Tracked Object Details" wrapped to three lines behind the back button, and it had no previous/next (the desktop arrows sit outside the dialog). Phones get a header with a one-line title and a previous/next segmented control, with the summary and share button on a full-width row beneath. Face Library, Classification and Chat, missing from both the phone nav bar and the command palette, are listed in the palette on phones. Flag `phoneFixes` | candidate |
| UI60 | web: live | hunk in `web/src/components/icons/LuIcon.tsx` | Frigate's config default for a camera group icon is `generic`, which names no lucide icon, so every group saved without picking one rendered as an empty pill in the Live header and sidebar. `generic` now shows the layers icon the command palette uses for camera groups. Flag `phoneFixes` | fork-only |
| C15 | web: camera images | hunk in `web/src/components/camera/CameraImage.tsx` | `CameraImage` requests frames at a height derived from its container; a frame scaled to that height has a rounded width, and in some layouts the 1 px change resizes the container, which asks for the other height. On phones Motion Tuner alternated 267/268 px and made about 34 frame requests a second (344 per 10 s, the same with the fork's phone fixes off; desktop 44). The requested height now only changes when it moves by more than 4 px. Found by `make demo-audit` (request storm) | candidate |
| C16 | web a11y | hunks in `web/src/components/fork/settings/SettingsNav.tsx`, `web/src/components/navigation/Bottombar.tsx`, `web/src/components/config-form/theme/templates/ObjectFieldTemplate.tsx`, `web/src/components/config-form/sections/OverrideDeltaPopover.tsx`, `web/e2e/helpers/settings-phone.ts` (group lookup scoped to the page); `web/src/utils/fork/a11y.test.ts`; hunk in `web/src/components/indicators/activity-indicator.tsx` (+ `__tests__/activity-indicator.test.tsx`); key in `web/public/locales/en/fork.json` | Two axe critical violations `make demo-audit` found on most pages: the Settings search input's `aria-controls` pointed at a result list that was only rendered while open (the list is now always rendered, hidden while closed), and the bottom-bar warning trigger was a `div` given `type="button"` and `aria-haspopup` (now a named `button`). Two upstream triggers rendered through `asChild` onto a `div` had the same problem and could not be reached by keyboard at all: the collapsible Settings card header (`ObjectFieldTemplate.tsx`) and the camera "overrides global" badge (`OverrideDeltaPopover.tsx`); both now have `role="button"`, `tabIndex={0}` and Enter/Space. The loading spinner (`ActivityIndicator`, used on 92 screens) put `aria-label` on a plain `div`, which axe reports as prohibited ARIA; it is now `role="status"`, so the label is valid and loading is announced. The unnamed bottom-bar Settings trigger is fixed by C13 | fork-only |
| D24 | tooling | `web/scripts/fork/phone-audit.mjs` (browser launch and file output), `web/scripts/fork/phone-audit-lib.mjs` (the checks, + `.test.js`); `demo-audit` target in `Makefile`; README section in `fork/demo/README.md` | `make demo-audit` signs in to the running demo stack as an Android phone (or `ARGS="--profile=desktop"`) and visits every page, recording console errors and warnings (including Chrome's Log-domain warnings Playwright's console event misses), exceptions, HTTP errors, axe-core WCAG 2.1 AA violations, sideways overflow, tap targets under 24/40 px, broken images, request storms (any URL over 8/s), long tasks and layout shift, and whether every menu/drawer closes on back without leaving the page. Writes `report.md`, `results.json` and screenshots to `fork/demo/.data/audit/`; `--update-baseline` saves a baseline so later runs list only new and fixed findings; `--strict` exits 1 on new findings. axe-core is installed into the gitignored `fork/demo/.data/tools`, not the web app's dependencies | fork-only |
| C14 | web: live | hunk in `web/src/components/player/JSMpegPlayer.tsx` | Leaving any jsmpeg live stream logged "WebSocket connection ... failed: Received a broken close frame containing a reserved status code" in Chrome (6 errors for a three-camera dashboard). jsmpeg closes its socket with no status code; the `ws4py` server records that as 1005 and echoes it back (`ws4py/streaming.py`, `websocket.py`), and 1005 must never be sent on the wire. The player now closes the socket with 1000 before destroying it, reading the socket from the player at cleanup (the single-camera view opens it after init), so the server echoes a valid code. Found reading Chrome's console against the demo stack | candidate |
| UI61 | web: phones | `web/src/components/fork/PhoneTimelineTabs.tsx`; hunk in `web/src/views/recording/RecordingView.tsx` | Desktop shows Timeline / Events / Detail as a toggle group in the recording header; phones had an unlabeled icon that opened a drawer, so the Detail timeline (detection boxes on the video) was easy to miss. Portrait phones get a segmented control between the video and the timeline with a sliding highlight; landscape keeps the drawer, whose timeline column is too narrow for three labels. Flag `phoneFixes` | fork-only |
| UI62 | web: phones | `web/src/components/fork/{PhoneBoxesToggle,PhoneDebugChips,PhoneDebugSheetControls}.tsx`, `web/src/lib/fork/phone-debug-sheet.ts`, keys in `web/public/locales/en/fork.json`; hunks in `web/src/views/live/LiveCameraView.tsx`, `web/src/views/settings/ObjectSettingsView.tsx` | Live Debug View (the frame with Frigate-drawn bounding boxes) was the last switch in a long phone drawer, and once on, its options panel filled the screen and left the frame a strip at the top. Phones get a one-tap boxes toggle in the camera header. In portrait the frame keeps the camera's aspect at the top with the overlays as a grid of toggle chips under it (`PhoneDebugChips`), and "All options" opens the full panel as a bottom sheet (upstream's panel restyled in place, so its markup is not moved). In landscape the frame fills the height and a frosted Overlays pill that counts active overlays opens the same sheet. Back closes the sheet; bounding boxes start on the first time. The portrait frame box is `min-h-0 overflow-hidden`: an `aspect-ratio` box otherwise grows to fit an image of the same aspect, and the 1 px rounding change made `CameraImage` re-request the frame about 23 times a second (found by a Chrome run against the demo stack; covered by an e2e test with a 1280x720 frame). Flag `phoneFixes` | fork-only |
| UI63 | web: phones | `web/src/hooks/fork/use-phone-detail-controls.ts` (+ `.test.ts`); hunk in `web/src/components/player/HlsVideoPlayer.tsx` | In Detail mode the box is drawn only while paused, and on phones the control bar sat across the lower third of the video, over the box and path. When playback pauses in Detail mode on a phone the controls step aside after a moment; a tap brings them back. Flag `phoneFixes` | fork-only |
| UI64 | web: tracking | `web/src/lib/fork/track-overlay.ts` (+ `.test.ts`), `web/src/components/overlay/__tests__/object-track-overlay.test.tsx`, `web/e2e/specs/fork/tracking-overlay.spec.ts`; hunks in `web/src/components/overlay/ObjectTrackOverlay.tsx`, `web/src/components/overlay/detail/TrackingDetails.tsx`, `web/src/fork/flags.ts` | The tracked-object overlay drew a box only within 10 ms of a recorded moment. Tapping a moment in Tracking Details kept the video playing, so its box never showed, and a paused frame between moments had none. Tapping a moment now pauses on it. Between moments the box sits on the object's path when the path points are at most 2 s apart; otherwise no box, rather than one in the wrong place. Path dots whose ground point is below the frame (close-up cameras) are faded, and a path pinned entirely to the edge is not drawn. Flag `trackOverlay` | fork-only |
| UI57 | web: phones | `web/src/lib/fork/viewport-zoom.ts`; hunks in `web/src/App.tsx` (call), `web/src/components/player/VideoControls.tsx` | `user-scalable=no` blocked pinch zoom on Android, where it buys nothing (it exists for iOS input focus zoom); it is dropped on Android, and views with their own pinch already `preventDefault()` two-finger touches. Playback control buttons (20 px icons) get a wider hit area on phones without moving the icons. Flag `phoneFixes` | candidate |
| UI18 | web: install | `web/public/images/maskable-icon-512x512.png`; hunk in `web/site.webmanifest` | Android launchers need a maskable icon of at least 192 px; the manifest offered a 180 px one and declared the 96 px notification badge `maskable` instead of `monochrome`. Adds a 512 px maskable icon with the bird inside the safe zone, marks the badge `monochrome`, and adds `description` and `id` (`./`, which resolves to `start_url`, so existing installs keep their identity). The splash stays white because the bird is black; no service worker or screenshots | candidate |
| UI58 | web: phones | `web/src/lib/fork/monaco-phone.ts`; hunk in `web/src/pages/ConfigEditor.tsx` | On a 412 px phone Monaco's minimap and gutters took a third of the width and long YAML lines scrolled sideways. Phones get word wrap, no minimap or folding gutter, and wider scrollbars. Flag `phoneFixes` | candidate |
| D23 | web tests | `web/e2e/helpers/settings-phone.ts`; hunks in `web/e2e/scripts/lint-specs.mjs` (settings exemption removed) and the five `web/e2e/specs/settings/*.spec.ts` | The spec linter exempted `specs/settings/`, so none of those specs had a phone test and the banned-pattern checks never ran there. Each now has a `@mobile-only` test that opens its screen the way a phone user does and checks the controls fit the 412 px width. Lifting the exemption exposed a Frigate+ test that had never run (an `if (count > 0) … else test.skip` branch around a wrong button label) and a deep-link check that could not fail (wrong heading case); both now assert for real, and fixed `waitForTimeout` waits became real conditions | fork-only |
| I18 | CI | hunks in `.github/workflows/fork-upstream-sync.yml` (`FORK_SYNC_TOKEN` checkout token, the bot's git identity, "Fork - Checks" dispatched only after a `GITHUB_TOKEN` push, an "Upstream sync failed" issue when a run fails) and the sync paragraph above | The daily sync failed on 2026-09-13 and 09-14 and `dev` fell 168 commits behind upstream: upstream's newer commits change `.github/workflows` (`actions/checkout` 7, `ci.yml`), and GitHub refuses a `GITHUB_TOKEN` push that changes workflow files, so `git push origin dev` failed before any issue was filed and the only sign was a red scheduled run. The pushes now use a `FORK_SYNC_TOKEN` secret (contents and workflows write) when it is set, and a failed run opens or comments on an "Upstream sync failed" issue. The rebase also had no git identity, which a hosted runner needs to replay commits. The owner has to add the secret. Found by a bug hunt | fork-only |
| D27 | backend: camera watchdog | hunk in `frigate/video/hwaccel_fallback.py` (`detect_cmd()` builds the software command from the live config; the cached copy is gone); case in `frigate/test/test_hwaccel_fallback.py` | D10's software fallback kept the command it built at the switch and reused it on every restart. A camera whose one input feeds detect and record carries its record output on that command, and turning recording on rebuilds the camera's commands through a `record` update, not an `ffmpeg` one, so the fallback was never reset and the restarted detect process wrote no segments until Frigate restarted. The command is now built when the detect process starts. Found by a bug hunt | fork-only |
| C17 | web: error handling | `web/src/api/fork/read-retry.ts` (+ `.test.ts`); hunk in `web/src/api/index.tsx` (`onErrorRetry`) | The global `errorRetryCount: 3` (G2) also capped the profile and config reads, and SWR's refresh skips a key in an error state, so a page opened while Frigate restarted (502s) stopped retrying after about a minute and `ProtectedRoute` kept its spinner until a reload (`AuthProvider` leaves loading only on a 401). Those two reads now keep retrying with SWR's backoff, capped at 30 s, while the server errors or is unreachable; a 4xx is final and every other read keeps the cap. Found by a bug hunt | fork-only |
| UI66 | web: settings | `web/src/lib/fork/zone-rename.ts` (+ `.test.ts`); hunks in `web/src/components/settings/ZoneEditPane.tsx` | Renaming a zone moved its name only in review alerts and detections. The snapshots, MQTT, GenAI and autotracking `required_zones` lists kept the old name, so they silently stopped matching the zone, and profile overrides stayed under the old name, which the backend's profile check rejects once the base zone is gone. Rename now moves the name in every list that deleting a zone already cleans (`PolygonItem`), and writes profile overrides back under the new name once the zone exists. Found by a bug hunt | candidate |
| E9 | security: camera access | hunks in `frigate/api/media_auth.py` (`role_has_full_access`, used by `check_camera_access` and `is_role_restricted`) and `frigate/api/auth.py` (`require_go2rtc_stream_access`); a case in `frigate/test/test_media_auth.py`; upstream's `test_unconfigured_role_can_access_any_stream` in `frigate/test/http_api/test_http_camera_access.py` becomes `test_unconfigured_role_is_denied_every_stream` | A role missing from `auth.roles` counted as full access in the media, clip, recording, export and go2rtc checks, while `User.get_allowed_cameras` and the websocket give it no cameras. `/auth` re-signs whatever role a JWT carries without checking the config, so a user whose restricted role was removed (for example with its only camera) kept a session that could open every camera's media. Admin and configured roles with an empty list keep full access; an unknown role now gets none. Login still falls back to `viewer` for a user whose role is gone, as upstream does. Found by a bug hunt | candidate |
| UI65 | web: live | hunk in `web/src/components/player/LivePlayer.tsx` (`playerError`); case in `web/e2e/specs/live-playback-errors.spec.ts` | The playback error card from the H.265 live startup fix passed player errors to the parent only for dashboard tiles that show a still, so the single-camera view never ran its fallback from MSE to WebRTC to jsmpeg: a stream the browser cannot decode through MSE (H.265 in Chrome) went straight to "Live video is unavailable", and Retry tried MSE again. Errors now reach the parent as well; a mode change resets the playback status, so the card stays only when the parent has nothing left to try. Found by a bug hunt | fork-only |
| UI67 | web: review | hunk in `web/src/pages/Events.tsx` (the `?id=` deep link); case in `web/e2e/specs/review.spec.ts` | Opening `/review?id=…` (push notifications, the inbox, links from Explore) wrote the review's day filter and then the recording as two history updates, and the second spread the state from before the first, so the day filter was dropped and the recording opened on the last 24 hours: for an older review the timeline and review list did not match the clip. Both now go in one update, as `onUpdateFilter` and share links already do. Found by a bug hunt | fork-only |
| D34 | fork: stability incidents | hunk in `fork/monitoring/incidents.py` (`resolve`, new `scope_removed`); a sentence in `fork/monitoring/README.md`; cases in `fork/monitoring/test_incidents.py` | An incident resolved only on a valid reading from its own camera or container, but the snapshot lists only enabled cameras and existing containers. A camera disabled or removed with a `capture:`, `detection:` or `recording:` incident open, or a `server:`, `memory:` or `restart:` incident on the `frigate-audio-trial` container after it was removed, stayed open forever, and the System page showed that dimension as needing attention for good. A fresh sample whose camera or container list was read and no longer names the scope now resolves it; a stale sample or one without the list still resolves nothing. Found by a bug hunt | fork-only |
| D35 | fork: stability incidents | hunks in `fork/monitoring/incidents.py` (`observe`, `advance_streaks`, `camera_problems`, the `capture` check in `resolve`); a sentence in `fork/monitoring/README.md`; cases in `fork/monitoring/test_incidents.py` | Every fresh sample reported each streak of three or more as a problem, measured or not, and streaks were never dropped. A disabled camera's `detection:` incident had its `updated` refreshed every 15 s and looked live, and on re-enabling a single skipped-frame reading reopened it; `ai:slow` did the same while the detector readings were missing. A streak now counts as a problem only when the latest sample measured it (a gap keeps the incident open without refreshing it, so it neither resolves nor flaps), and a camera that leaves the sample loses its streak. Also, the 20 s capture grace timer (`capture_missing`) was cleared only when FPS rose above 0, so after a long restart a camera still at 0 FPS opened `capture:` at once; a stats gap or a camera leaving the sample now restarts the grace, and a 0 FPS reading no longer resolves an open capture outage. Found by a bug hunt | fork-only |
| D36 | fork: audio trial | hunks in `fork/audio_trial/worker.py` (`process_job`, `process_pending`) and `fork/audio_trial/queue_store.py` (new `Queue.release`); a sentence in `fork/audio_trial/README.md`; cases in `fork/audio_trial/test_worker.py` | An interrupted inference (camera-priority preemption, time limit, a non-zero child exit, or a worker stop, each returned as a partial result when a checkpoint exists) was finished as done with an empty transcript and failed stages, so the single retry and the restart recovery the README promises never applied. The first interruption now returns the job to the queue through `queue.fail`, keeping the checkpoint so the retry resumes completed stages, and the partial result is kept only once the retry is spent. A service stop, whether it interrupts inference or fails the job earlier, returns the job with its attempt refunded and records no audio failure, so restarting the service no longer spends the retry. Found by a bug hunt | fork-only |
| D30 | backend: camera watchdog | hunk in `frigate/video/ffmpeg.py` (`CameraWatchdog.run`, the `ffmpeg` update); case in `frigate/test/test_hwaccel_fallback.py` | D14 promises hardware decoding is tried again as soon as a camera's ffmpeg settings change, but the watchdog reset the software fallback only when an `ffmpeg` update arrived while the camera was enabled. A disabled camera in fallback whose stream or ffmpeg settings changed kept the fallback and its remembered state file, and came back decoding in software when re-enabled. The fallback is now reset on every `ffmpeg` update; ffmpeg still starts only for an enabled camera. Found by a bug hunt | fork-only |
| D31 | backend: recording | `frigate/record/move_failures.py` (new, + `frigate/test/test_record_move_failures.py`); hunks in `frigate/record/maintainer.py` (`__init__`, `move_files`, the failed-conversion and exception paths of `move_segment`); `frigate/test/test_recording_write_safety.py` setup | The recording write protection (949713b29) keeps a segment's cached copy when its conversion fails, so a passing problem can be retried, where upstream deletes it. A segment that failed every time (ffmpeg hanging until the 60 s timeout, `os.replace` raising, ffmpeg rejecting the file) was then retried forever: each hang held up every camera's segments for a minute, the camera's object and audio info is only trimmed up to its oldest cached segment and grew without bound, and nothing dropped it until more than six segments were stuck. Failed attempts are now counted per cached segment, and after three the segment is dropped with a warning; a single failure still keeps it for a retry. Found by a bug hunt | fork-only |
| D32 | backend: review | hunks in `frigate/review/maintainer.py` (`run`, the LPR complete and start branches for an existing segment); `frigate/test/test_review_lpr_segment.py` (new) | A plate read from a dedicated LPR camera on a segment already in progress forced the segment's severity to detection and, when the plate completed, wrote `last_alert_time` instead of `last_detection_time`. An alert in progress on that camera was downgraded to a detection (and kept only for detection retention), then ended early from its stale detection time. A plate now leaves the severity alone, since detection is the lowest, and extends the detection time. Found by a bug hunt | candidate |
| D33 | backend: previews | hunk in `frigate/output/preview.py` (`PreviewRecorder.__init__`, the startup scan of cached frames); case in `frigate/test/test_preview_loader.py` | At startup each camera's preview recorder picked up its cached frames by the prefix `preview_{camera}-`, but camera names may contain `-`, so camera `cam` also matched `cam-1` and `cam-2`. It deleted this hour's frames of cameras that sort before it (`cam-1`) and added the timestamps of those that sort after (`cam-2`) to its own list, so at the end of the hour ffmpeg was asked for files that do not exist and `cam` lost that hour's preview. The scan now takes the camera name from the file name and requires an exact match. Found by a bug hunt | candidate |
| I19 | docs + docker: rootless cache | hunks in `docs/docs/frigate/installation.md`, `docs/docs/guides/getting_started.md` and `docs/docs/troubleshooting/faqs.md` (the `/tmp/cache` tmpfs), `docker/main/rootfs/etc/s6-overlay/s6-rc.d/prepare/run` (the cache check logs the fix) and `frigate/util/atomic.py` (`ensure_private_directory` names both UIDs); cases in `frigate/test/test_atomic_private_file.py` and `fork/scripts/test_rootless_runtime.py` | The main image runs as UID 65534 and its prepare step refuses a `/tmp/cache` it does not own, but the install docs mounted the cache with `--mount type=tmpfs` or a Compose `type: tmpfs` volume, which Docker always creates owned by root (mode 1777) with no way to set an owner. A container set up from the docs stopped in prepare with a bare traceback, and go2rtc and Frigate never started. The docs now use `--tmpfs` or the Compose service-level `tmpfs:` list with `uid=65534,gid=65534,mode=0700`, and say that images run as root (ROCm and the other hardware variants) leave the owner out. Prepare now logs the owner it found and the mount to use instead of a traceback, and the runtime check mounts the documented tmpfs and verifies that a root-owned one is refused with that message. Found by a bug hunt | fork-only |
| I20 | CI | hunks in `.github/workflows/fork-upstream-sync.yml` (`git rebase --rebase-merges`, the rebase output kept for the issue) and the sync paragraph above | The sync rebased `next` onto `upstream/dev` without `--rebase-merges`, which flattened every pull request merge into a line of commits replayed in an order they were never written in, so `sync/upstream` did not have the shape `next` is kept in and could not stand in for its rebase. The merges are now kept. Git redoes each merge, so a merge that needed a hand resolution conflicts again and is reported, rather than being replayed without its resolution. A rebase that failed for another reason (a git error, a bad ref) was reported as a conflict with an empty file list; the issue now quotes git's error. Found by a bug hunt | fork-only |
| I21 | tooling: promote | hunks in `fork/scripts/promote.sh` (a rebased copy on `next` counts, `--drop-main-commits`); `fork/scripts/test_promote.py` (new) | `make promote` refused while `main` had a commit with no patch-equivalent on `next` (`git cherry`). Rebasing `next` onto upstream with conflict edits changes those patches, so afterwards every edited commit looked missing and promote refused for good, telling the owner to merge commits `next` already had, with no way out but a manual push to `main`. A commit now also counts as on `next` when `next` has a copy with the same author, author date and subject, which a rebase or cherry-pick keeps. For a change `next` has in another form (upstream's version of a backport), `--drop-main-commits` lists the commits and promotes over them only after the owner types `drop`, which `--yes` does not skip. A commit pushed only to `main` is still refused. Skipping commits reachable from the previous release tag would not do: every push to `main` is tagged, so it would hide a direct push too. Found by a bug hunt | fork-only |
| D28 | backend: recordings API | hunk in `frigate/api/record.py` (`no_recordings`); a case in `frigate/test/http_api/test_http_media.py` | `GET /recordings/unavailable` filled a missing `before` or `after` (or a 0) with `datetime.datetime.now()` and `datetime.timedelta`, but the module imports the `datetime` class itself, so those names do not exist and the request failed with a 500 unless the caller sent both bounds. The defaults now use the imported `datetime` and `timedelta`, giving the hour that ends now as intended. Found by a bug hunt | candidate |
| D29 | backend: PTZ API | hunk in `frigate/api/media.py` (`camera_ptz_info`); `frigate/test/http_api/test_http_ptz_info.py` (new) | `GET /{camera}/ptz/info` is an `async` handler that sent `get_camera_info` to the ONVIF controller's loop and then waited with `future.result()`, which blocks the API's event loop. While an ONVIF camera was slow or unreachable (the call also tries to reconnect), every other async API request stalled with it. The handler now awaits the future with `asyncio.wrap_future`. The controller's loop runs on its own thread, so awaiting it cannot deadlock, and no timeout is added: the handler used to wait without one and a slow camera now only delays its own request. Found by a bug hunt | candidate |
| E10 | security: exports | hunk in `frigate/api/export.py` (`export_recording_custom`); cases in `frigate/test/http_api/test_http_export.py` | Attaching an export to an existing case is admin-only on `POST /export/{camera}/...` and `POST /exports/batch` until cases have their own access control, but `POST /export/custom/{camera}/...` accepted `export_case_id` from any user with access to the camera, so a non-admin could still add exports to any case. The custom route now returns the same 403 for a non-admin that sends a case id; the admin check it already made for ffmpeg arguments moves up to cover both. The web UI does not call this route. Found by a bug hunt | candidate |
| UI75 | web: settings | hunks in `web/src/views/settings/MotionTunerView.tsx` (`saveToConfig`, `onCancel`); `web/e2e/specs/settings/unsaved-guard.spec.ts` | The motion tuner set the Settings page's unsaved-changes flag on every edit and never cleared it: save and reset cleared only the view's own `changedValue`, so after a successful save or a reset the navigation guard (`useUnsavedNavigation`) asked about unsaved changes on every exit from Settings. A successful save and a reset now clear the flag. Found by a bug hunt | fork-only |
| UI76 | web: settings | hunks in `web/src/views/settings/TriggerView.tsx` (the `finally` of `saveToConfig` and `onDelete`, the rename `catch` in `onEdit`); case in `web/e2e/specs/settings/unsaved-guard.spec.ts` | Creating, editing and deleting a trigger set the page's unsaved-changes flag before the request and cleared it only on success, so after a failed save or delete (an embedding or validation error, the network) the navigation guard asked about unsaved changes on every exit from Settings until the camera changed. The flag now clears once the request settles, whatever the outcome. Found by a bug hunt | fork-only |
| UI77 | web: settings | hunk in `web/src/views/settings/MasksAndZonesView.tsx` (an effect on `editPane`); case in `web/e2e/specs/settings/unsaved-guard.spec.ts` | The masks and zones view only ever cleared the page's unsaved-changes flag and nothing set it, so a half-drawn zone, moved mask points or edited zone fields were lost without a prompt when leaving Settings. The flag is now set while a zone or mask editor is open and cleared when the editor closes (save or cancel) or the view unmounts. Found by a bug hunt | fork-only |
| UI79 | web: settings | `web/src/lib/fork/settings-diff.ts` (+ `.test.ts`) | The Review dialog diffed `config.model` and `config.detectors` from /api/config against the pending form data, but /api/config adds runtime fields (colormap, attribute lists, Frigate+ data, each detector's merged labelmap), so picking a Frigate+ model listed dozens of rows like `colormap.person: [...] -> (default)` while Save All writes only `path`. Both sides now drop the hidden fields Save All strips, and a key the pending data lacks is listed as removed only when Save All clears both sections first (a detector added, removed or renamed, or a switch between a Frigate+ and a custom model). Found by a bug hunt | fork-only |
| UI78 | web: settings | `web/src/hooks/fork/use-prune-settings-state.ts` (+ `.test.ts`); hunk in `web/src/pages/Settings.tsx` (import and one call) | Deleting a profile never dropped the cameras' `editingProfile` entries or the `camera::profiles.<name>.*` pending edits, and the section pages use `editingProfile` without checking the profile exists, so Save wrote `cameras.<camera>.profiles.<deleted>.*`, which the backend rejects ("not defined in the top-level 'profiles' section"), and the pending state and the navigation guard stayed stuck. Deleting a camera in Camera Management likewise left its pending sections, so Save All re-created the camera with no inputs. Once the config no longer has a profile or camera, the entries that name it are dropped. Found by a bug hunt | candidate |
| UI74 | web: settings | `web/src/lib/fork/zone-rename.ts` (+ `.test.ts`); hunks in `web/src/components/settings/ZoneEditPane.tsx` (`saveToConfig`); case in `web/e2e/specs/fork/zone-editing.spec.ts` | Renaming a zone ran as separate requests: delete the old zone, then write the new one (and, since UI66, a third for profile overrides). When the write failed (a validation error, the network), an error toast showed but the zone, with its review entries, was already gone. `config/set` applies a whole `config_data` body before it validates and restores the file when validation fails, so the delete, the new zone, the moved required_zones entries and profile overrides now go in one body, and a rejected rename changes nothing. Found by a bug hunt | candidate |
| UI68 | web: status bar | `web/src/lib/fork/stats-staleness.ts` (+ `.test.ts`), `web/src/hooks/use-stats.test.tsx`; hunk in `web/src/hooks/use-stats.ts` (stale-stats check) | The stale-stats warning compared the server's `service.last_updated` with the browser's clock over a fixed 90 s window and then returned early, skipping every other status-bar check. Frigate publishes `stats` once per `mqtt.stats_interval` (15 s or more), so with an interval of 120 or 300 the warning was permanent and offline cameras, slow detectors, low shm, high CPU and the debug replay notice were never shown; a tablet whose clock ran more than 5 s behind the server did the same. Staleness now counts from when the last stats message reached the browser, allows 1.5 intervals (at least interval + 15 s), and the other checks still run on the last stats received. Found by a bug hunt | fork-only |
| UI69 | web: review | hunk in `web/src/hooks/fork/use-restored-scroll.ts` (effect cleanup); case in `web/src/hooks/fork/use-restored-scroll.test.tsx` | When a list's key changed (Review's Alerts and Detections, Explore's tabs), the effect cleanup saved `scrollTop` for the old key, but React runs that cleanup after the new, shorter list is already in the DOM, so the browser had clamped the value (often to 0) and it overwrote the position `onScroll` had saved. Scrolling Alerts far down, switching to Detections and pressing Back left Alerts near the top. The cleanup no longer saves; `onScroll` keeps each key's position current. Found by a bug hunt | fork-only |
| UI70 | web: live | `web/src/lib/fork/peer-connection.ts`, `web/src/components/player/WebRTCPlayer.test.tsx`; hunks in `web/src/components/player/WebRTCPlayer.tsx` (import, effect cleanup) | The WebRTC player's cleanup closed the peer connection but never stopped the microphone track that `getUserMedia` captured for two-way talk (it is not kept with the video tracks), so after turning two-way talk off or leaving the camera the microphone stayed captured and the browser kept its recording indicator until the tab closed. The cleanup now stops every sender track before closing the connection. Found by a bug hunt | candidate |
| UI71 | web: live | hunks in `web/src/components/player/WebRTCPlayer.tsx` (`PeerConnection`, `connect`, the connection effect); case in `web/src/components/player/WebRTCPlayer.test.tsx` | Building the WebRTC peer connection can wait on the browser's microphone prompt, and nothing cancelled it. When the effect was cleaned up first (playback stopped, two-way talk toggled, the camera left), the cleanup found no connection to close, and the stale call then attached its stream to the video, created the peer connection and opened a WebSocket nobody closed, streaming with the microphone open. The effect now sets a cancelled flag in its cleanup; a call that finishes after it stops its tracks, closes its connection and opens no socket. Found by a bug hunt | candidate |
| UI72 | web: live | hunk in `web/src/components/player/WebRTCPlayer.tsx` (the 5 s startup timer); cases in `web/src/components/player/WebRTCPlayer.test.tsx` | The WebRTC player's "connection timed out" timer started on mount whether or not playback was enabled, and only `loadeddata` cleared it. A dashboard tile in WebRTC mode that shows a still while the camera is idle mounts with playback off, so after 5 s it reported "stalled" and the dashboard (`LiveDashboardView`, `DraggableGridLayout`) switched that camera to jsmpeg; WebRTC was almost never used on the dashboard. The timer now runs only while playback is enabled and is cleared when playback turns off. The single-camera view always plays, so its timing is unchanged, and a real stall still reaches the parent through UI65. Found by a bug hunt | candidate |
| UI73 | web: recordings | hunks in `web/src/lib/fork/timeline-scrubber.ts` (`SNAP_SEGMENTS`, `snapMaxDistance`) (+ cases in `.test.ts`) and `web/src/components/timeline/ReviewTimeline.tsx` (snap on release); case in `web/e2e/specs/fork/timeline-scrubber.spec.ts` | UI8's snap on handlebar release called `snapToNearestEvent` without the `maxDistance` the helper supports, so every release jumped to the nearest review start however far away it was: dropping the playhead at 14:30 in a motion-only stretch moved it to a 09:12 alert. A release now snaps only when a review starts within 4 segments (32 px, 2 minutes at the default zoom) and otherwise stays where it was dropped. Found by a bug hunt | fork-only |
| UI80 | web: review | hunk in `web/src/pages/Events.tsx` (the Undo callback in `markItemsAsReviewed`); case in `web/e2e/specs/fork/bulk-actions.spec.ts` | Undo after "Mark these items as reviewed" only called `reloadData`, which moves the last-24-hours window and refreshes the summary. With a date range the review list's key comes from the range, so nothing was read again and the page kept the copies that marking had set to reviewed: after Undo on a past day the items stayed marked reviewed, or hidden, until a reload. Undo now also clears and reads the list again, as the key change does for the last 24 hours. Undo from the selection bar still relies on `reloadData`, as upstream's mark there does. Found by a bug hunt | fork-only |
| UI81 | web: inbox | `web/src/lib/fork/inbox-store.ts` (+ `.test.ts`) | `ingestReview` added any message other than `end` for a review it did not have. The `reviews` topic sends `update` messages while a review is active, and the collector re-ingests the last message when it remounts, so dismissing an item or pressing Clear while the alert was still active brought it back as unread. Only a review's `new` message adds an item now, and dismissed or cleared ids are remembered in localStorage (the newest 500) and never added again. Found by a bug hunt | fork-only |
| UI87 | web: review | `web/src/lib/fork/bulk-actions.tsx` (`changedReviewIds`, + `.test.ts` case); hunks in `web/src/components/filter/ReviewActionGroup.tsx` (`onToggleReviewed`) and `web/src/pages/Events.tsx` (`markItemsAsReviewed`); cases in `web/e2e/specs/fork/bulk-actions.spec.ts` | Undo after "Mark as reviewed" also unmarked items that were reviewed before. The selection bar sent every selected id, so with a mixed selection every item was marked reviewed and Undo sent `reviewed: false` for all of them; "Mark these items as reviewed" did the same with Show Reviewed on. Only the ids whose state changes are sent now (in `markItemsAsReviewed` they are taken before the cache is marked), so Undo reverts just those, and the toast counts them. Found by a bug hunt | fork-only |
| UI82 | web: logs | hunk in `web/src/pages/Logs.tsx` (`fetchInitialLogs`); case in `web/e2e/specs/logs.spec.ts` | With a severity filter the page reads the log from its first line (`start: 0`), but it still set the history index to `totalLines` minus the lines kept, which is above 0 whenever the filter dropped lines. Scrolling up then fetched ranges already on screen and added them again as duplicates. The index is now 0 when the read started at the first line. Found by a bug hunt | candidate |
| UI83 | web: logs | hunk in `web/src/pages/Logs.tsx` (`handleCopyLogs`); cases in `web/e2e/specs/logs.spec.ts` | Copy re-ran the page's load (`fetchInitialLogs`), which UI50 changed to return `false` instead of throwing, so its `.catch` never ran. The text copied was the lines from before the read, the view flashed a spinner and reloaded, and a failed read replaced the log with the error screen. Copy now reads the log into a local variable without touching the page, copies those lines, and shows the copy error toast when the read fails. Found by a bug hunt | candidate |
| UI85 | web: faces | `web/src/hooks/fork/use-leave-missing-page.ts` (+ `.test.ts`); hunks in `web/src/pages/FaceLibrary.tsx` (`faceImages`, the hook call) and `web/src/views/classification/ModelTrainingView.tsx` (the hook call); case in `web/e2e/specs/face-library.spec.ts` | The face library went back to "Recent Recognitions" only when the face had exactly one image, but the backend removes a face's folder once it is empty, after a bulk delete (`frigate/embeddings/__init__.py`) or when its last image is reclassified (`frigate/api/classification.py`). The page stayed on the removed face ("alice (0)", gone from the dropdown, Upload still aimed at it) and its image list became undefined, so Ctrl+A threw "faceImages is not iterable". The page now goes back to the training page whenever newly loaded data no longer has the open face, and the list defaults to empty. A classification model's training view had the same problem with an emptied category (the backend removes empty categories other than "none") and uses the same hook. The check runs only when the data changes, so a rename, which moves the page before the data has the new name, is not undone. Found by a bug hunt | candidate |
| UI86 | web: live | `web/src/hooks/fork/use-delete-user-key.ts`; hunks in `web/src/components/filter/CameraGroupSelector.tsx` (`NewGroupDialog`: the layout delete and `onDeleteGroup`); cases in `web/e2e/specs/live.spec.ts` | Deleting a camera group erased the layout of the group being viewed, not the deleted one. The dialog's delete was bound to `${activeGroup}-draggable-layout`, the key the dashboard grid reads, and it also cleared the saved group, both before the `config/set` request and whether or not it succeeded: deleting "back" while viewing "front" lost front's layout and saved group and left back's layout behind. After a successful save the deleted group's own layout is removed, and the saved group is cleared only when it was the open group. Found by a bug hunt | candidate |
| UI84 | web: logs | hunk in `web/src/pages/Logs.tsx` (`handleScroll` and a `fetchingHistoryRef`); case in `web/e2e/specs/logs.spec.ts` | Scrolling up set `isLoading` and cleared it again before the older range arrived, and React batched the two, so the `!isLoading` guard never held: while a slow read was in flight, further scroll events requested and prepended the same range again. A ref now marks a history read as in flight until it settles. The loading state is no longer touched, since holding it would swap the log for a spinner. Found by a bug hunt | candidate |
| D37 | backend: media API | hunk in `frigate/api/media.py` (`vod_event`, the `has_clip` check); `TestHttpVodEvent` in `frigate/test/http_api/test_http_media.py` | `vod_event` is meant to set `has_clip` to false when an event that started more than 5 minutes ago has no recordings left, but it looked for a `(body, 404)` tuple while `vod_ts` returns a `JSONResponse`, so the check never matched and such events kept offering a clip that cannot play. It now checks for a `JSONResponse` with status 404. Found by a bug hunt | candidate |
| D38 | backend: media API | hunk in `frigate/api/media.py` (`label_thumbnail`); `TestHttpLabelThumbnail` in `frigate/test/http_api/test_http_media.py` | `GET /{camera}/{label}/thumbnail.jpg` (and `best.jpg`) is meant to return a blank placeholder JPEG when the camera has no matching event, but it looked up the newest id with `MAX()`, which returns `None` rather than raising the `DoesNotExist` the placeholder branch catches. It passed `None` on to `event_thumbnail` and answered with a 404 JSON error. A missing id now returns the placeholder. Found by a bug hunt | candidate |
| D39 | backend: events API | hunk in `frigate/api/event.py` (`_review_thumb_paths_for_events`); case in `frigate/test/http_api/test_http_event.py` | G4 replaced the JSON LEFT JOIN in `/events/search` with a windowed review segment lookup that required `end_time` to be at or after the window start. A review still in progress has no end time, so it never matched and its events came back with `thumb_path: null`, where the old join included them. Segments with no end time match again. Found by a bug hunt | fork-only |
| D40 | backend: review API | hunk in `frigate/api/review.py` (`review_summary`: one clause list, with the 24 hour bound added to `last_24_query` only); case in `frigate/test/http_api/test_http_review.py` | `/review/summary` built its filters twice. The `last24Hours` query applied the `zones` filter and matched labels against both `objects` and `audio`, but the per-day counts dropped the zone filter and matched labels only against `objects`. With `?zones=driveway` the day totals counted every zone, and a filter on an audio label found no days at all, so the calendar disagreed with the 24 hour totals. Both queries now use one clause list. Found by a bug hunt | candidate |
| D41 | backend: media API | hunk in `frigate/api/media.py` (`latest_frame`, the `frame is None` check moved above the size calculation); case in `frigate/test/http_api/test_http_latest_frame.py` | `latest_frame` read `frame.shape` before its `frame is None` check. With the camera offline, no preview frame, and `camera-error.jpg` missing, the request raised `AttributeError` and answered 500 with a traceback instead of the intended "Unable to get valid frame". The check now runs first. Found by a bug hunt | candidate |
| I22 | CI: releases | hunks in `.github/workflows/fork-build.yml` (the version step and the release job's condition) | The release was not idempotent under "Re-run all jobs": the version comes from the build date and `GITHUB_RUN_NUMBER`, so a re-run the same day failed because `fork/<version>` already existed, and a re-run on a later day tagged the image with a new version and published a second `--latest` release for the same commit, whose notes (diffed against the first) said "No user-facing changes". A main build now looks for a release whose notes carry the `fork-build: <sha>` marker for its commit; when one exists it reuses that version, so the image tags match the release, and the release job is skipped. Found by a bug hunt | fork-only |
| I23 | tooling: release notes | hunks in `fork/scripts/release_notes.py` (`patch_ids`, `fork_commits`, `build`, `main`); cases in `fork/scripts/test_release_notes.py` | "Already released" was decided by an exact subject match, not commit identity, so a new commit whose subject repeated an earlier fork subject (such as "Fix lint") was dropped from the notes, neither listed nor counted. A commit is now left out only when the previous release has one with the same `git patch-id`, which survives a rebase, or the same author, author date and subject, which survive a conflict edit that changes the patch (as in I21). The notes count what they leave out, and the release step's log prints that count. Found by a bug hunt | fork-only |
| I24 | tooling: checks | hunks in `fork/scripts/check.sh` (`ruff_paths`, `py_files`, `py_gates`), `fork/scripts/py-checks.sh` (`fork_suites`, the plain unittest run), `Makefile` (`lint`, `format`), `fork/README.md` | The local "same gates as CI" check left out `fork/audio_trial` and `fork/monitoring`, which CI lints with ruff and tests in the test image: `make check` never ran ruff or their tests, and `make check-fast` also left them out of its Python change detection and reported "no backend changes", so a change there passed locally and failed CI. Both are now in the ruff paths (`make lint` and `make format` too) and the fast-mode detection, and a plain `py-checks.sh` run also discovers the `audio_trial`, `audio_trial/benchmarks` and `monitoring` suites, as CI's coverage run does. CI's audio companion job, which builds the companion's own image, is still not mirrored. Found by a bug hunt | fork-only |
| I25 | CI: type ratchet | hunks in `fork/scripts/ci-changes.sh` (`web_re`), `web/scripts/fork/type-ratchet.mjs` (`raisedAbove`, `readBaseBaseline`), `.github/workflows/fork-checks.yml` (`web_lint`: the base fetch, `TYPE_RATCHET_BASE`), `fork/scripts/check.sh` (`gate_ratchet`); cases in `web/scripts/fork/type-ratchet-cli.test.js` | `web_re` did not include `fork/type-ratchet.json`, which the `web_lint` job reads through `npm run type-ratchet`, so a commit that only edited the baseline skipped the ratchet in CI, and the ratchet compared the counts against a baseline the same pull request could raise, so nothing checked the baseline against the base branch. Both baselines under `fork/` now count as web changes, and the ratchet fails when a baseline value is higher than the one at `$TYPE_RATCHET_BASE` (default `origin/next`). In CI a pull request fetches its base commit and compares with it; a push skips the comparison, since an upstream rebase landing on `next` may have to raise the baseline. `make check` compares with the merge base (an empty `TYPE_RATCHET_BASE` skips it), and a base without a readable baseline is reported and skipped. Found by a bug hunt | fork-only |
| D42 | backend: recording | hunk in `frigate/record/maintainer.py` (`move_files`, the `asyncio.gather` of segment moves); case in `frigate/test/test_maintainer.py` | Backport of upstream's handling. `move_files` gathered every segment's move without `return_exceptions`, so one segment that raised (a "database is locked" while reading its reviews, or `os.makedirs`, which sits outside the move's `try`) made the gather raise and the results of the segments that had already finished were dropped. Those segments were already copied to the recordings directory with their cached copy deleted, so they never got a database row, did not show anywhere, and were later swept as orphans. Each failure is now logged and skipped, and the other segments are inserted. Found by a bug hunt | upstream #24009 (merged on upstream `dev`; this row goes when `next` rebases onto 0.19) |
| D43 | backend: time | hunk in `frigate/util/time.py` (`get_dst_transitions`); cases in `frigate/test/test_time_zones.py` | `get_dst_transitions` probed the offset once every 24 h from the start time and reported a change at the first probe after it, up to a day late: for New York starting at local midnight on 2024-03-10, the 07:00Z transition was reported at 2024-03-11 05:00Z, so about 22 h of events, review items and recordings were put into days with the old offset. A transition after the last daily probe was not found at all. The end time is now probed too, and a probe that sees the offset change bisects the interval to the second of the transition. The existing test that expected the daily boundary now expects the transition itself. Found by a bug hunt | candidate |
| D44 | backend: recording | hunk in `frigate/record/cleanup.py` (`expire_recordings`, the review query); `frigate/test/test_record_cleanup_pre_capture.py` (new) | The reviews used to decide whether an expiring recording overlaps an alert or detection were limited to those starting before the continuous-retention cutoff, but a review also keeps the pre-capture seconds before its start. With continuous retention shorter than alert retention, an alert starting within `pre_capture` seconds after the cutoff was left out, so its pre-roll segment (no motion, ending just before the cutoff) was deleted while the alert was kept. The query now reaches the cutoff plus the camera's largest pre-capture (`record.event_pre_capture`). Found by a bug hunt | candidate |
| D45 | fork: audio trial | hunks in `fork/audio_trial/worker.py` (new `recent_large_runs` and `defer_large`, `process_job`) and `fork/audio_trial/queue_store.py` (`Queue.defer` takes `refund`); a sentence in `fork/audio_trial/README.md`; cases in `fork/audio_trial/test_worker.py` | A deferred `second_opinion` job downloaded its clip again (up to 100 MB) and re-ran ffmpeg before checking the hourly Large budget and `health(large=True)`. While memory was short or both hourly runs were used, it was claimed again every minute or so for up to an hour, downloading each time, and each claim added an attempt, so a later transient failure went straight to failed. A job with a stored Medium result now checks the budget and memory before the download, and a deferral at that point returns the attempt its claim spent. Found by a bug hunt | fork-only |
| D46 | backend: AI model history | hunks in `frigate/stats/model_history.py` (new `history_sample`, `save_sample`, `read_history`); case in `frigate/test/test_model_health_followup.py` | Every minute the whole `/ai/models` response was stored, including `server.stability` (up to 100 incidents and 120 samples), and `/ai/models/history` returned all of about 1440 rows, so the history database and each response grew to tens of MB. The history graphs (`appendHistory`, `AIModelGraphs`) read only `updated`, `models`, `shared_gpus` and `audio`; `server` is shown only from the live response. Stored samples now leave out `server`, and rows stored before this are trimmed when read. A sample without `server` is still a valid `AIModelsResponse`, whose `server` defaults to empty. Found by a bug hunt | fork-only |
| D47 | backend: share links | `frigate/events/share_links.py` (new, + `frigate/test/test_share_link_cleanup.py`); hunk in `frigate/events/cleanup.py` (`run`, one call and its import) | Clip share links (UI11) were never deleted, not on expiry and not when retention deleted their event, so expired and orphaned tokens piled up in `ShareLink`. Each event cleanup pass (every 5 minutes, after events are deleted) now removes links whose event no longer exists and links that expired more than 7 days ago. Link validation is unchanged: a recently expired link still answers 410 "expired", and a link with no event already answered 404. Found by a bug hunt | fork-only |
| UI88 | web: status bar | hunk in `web/src/hooks/use-stats.ts` (model server check); cases in `web/src/hooks/use-stats.test.tsx` | The "model server unavailable" warning tested `models?.server?.status !== "connected"`, which is also true while `models` is undefined: before `ai/models` first resolves and when that request fails. The backend always sends `server`, so admins saw a false warning for a moment on every app load. It now shows only once the response has loaded and its server is not connected; a pending or failed request is already reported by the readiness warning above it. Found by a bug hunt | fork-only |
| C18 | web: error toasts | `web/src/api/fork/read-error-toast.ts` (`readErrorKeyId`) (+ cases in `.test.ts`) | `readErrorKeyId` pulled the path out of SWR's serialized `@"path",#...` keys, but `useSWRInfinite` (Explore's search) and `useSWRSubscription` prefix their keys with `$inf$` and `$sub$`, so the pattern never matched. The toast read `Failed to load $inf$@"events/search",#... (HTTP 500)` and the cooldown kept a separate bucket for every page of the key. The prefix is now dropped before the path is read. Found by a bug hunt | fork-only |
| UI90 | web: recordings | hunk in `web/src/components/player/dynamic/DynamicVideoController.ts` (`seekToTimestamp`); `DynamicVideoController.test.ts` (new) | `seekToTimestamp` seeked only when `seekSeconds != 0`, so a valid seek to player position 0, the first recording segment's start, did nothing: the player stayed where it was and `play=true` was ignored. It now bails out only when there is no position (`undefined`), and seeks to 0 like any other time. Found by a bug hunt | candidate |
| UI89 | web: recordings | hunk in `web/src/components/player/dynamic/DynamicVideoController.ts` (`seekToTimestamp`); cases in `DynamicVideoController.test.ts` | Seeking to a time inside the current hour chunk but before its first recording segment made `calculateSeekPosition` return undefined, and the controller only set "No recordings found for this time", without pausing or seeking. The message sat over video that kept playing from its old position until the next `playing` event. Such a time now plays from the first segment's start, as a time in a gap between segments already plays from the next one, and a seek that lands clears an earlier "no recordings" state. Found by a bug hunt | candidate |
| UI91 | web: settings | hunk in `web/src/components/settings/ZoneEditPane.tsx` (`saveToConfig`, the friendly name query); case in `web/e2e/specs/fork/zone-editing.spec.ts` | A zone's friendly name was sent only when it differed from the zone id and was never deleted, and a save merges into the saved zone, so setting the name back to the id kept the old one ("Front Yard"). When the name equals the id and a name is saved at the path being written (the base zone, or the profile's override when editing a profile), the save now deletes it. A rename already drops it, since it removes the old zone in the same request (UI74), and the form does not accept an empty name. Found by a bug hunt | candidate |
| UI92 | web: config editor | hunks in `web/src/pages/ConfigEditor.tsx` (a theme effect; the change listener registered where the model is made, and the separate listener effect removed); cases in `web/e2e/specs/config-editor.spec.ts` | The editor effect depended on `theme` and `systemTheme`, so a theme change (or the OS switching dark mode with the theme set to system) disposed the editor and model and made them again from the saved config, which wiped typed edits and left `hasChanges` stale. The `onDidChangeContent` listener was registered only when `rawConfig` changed, so it stayed on the disposed model and later edits never engaged the unsaved-changes guard. The theme now changes in place with `monaco.editor.setTheme`, and the listener is registered, and disposed, with each model. Found by a bug hunt | candidate |
| UI93 | web: review | hunk in `web/src/pages/Events.tsx` (the `group` search effect); case in `web/e2e/specs/review.spec.ts` | The `group` search effect read `group.cameras.length` before its `if (group && ...)` check, so a bookmarked or shared `/review?group=` link to a camera group that was deleted or renamed threw a TypeError and the error boundary replaced the Review page. The effect now drops the param and returns when the config has no such group. Found by a bug hunt | candidate |
| UI94 | web: system | `web/src/hooks/fork/use-metric-time.ts`, `web/src/views/system/AIModelMetrics.test.tsx`; hunks in `web/src/views/system/{AIModelMetrics,AIModelGraphs,MetricHistoryTable,StabilityIncidents}.tsx` and `MetricHistoryTable.test.tsx` (partial i18n mock) | The AI model page formatted the model's last use, the graph axis and tooltip, the graph history table and the stability evidence table in the browser's time zone, ignoring `ui.timezone`, and the stability table also ignored the app language. A hook now formats these times with `formatUnixTimestampToDateTime`, `ui.timezone`, `ui.time_format` and the current language, as the other system views do. Found by a bug hunt | fork-only |
| UI95 | web: system | `web/src/views/system/ServerPressure.test.tsx`; hunks in `web/src/views/system/ServerPressure.tsx` and `web/public/locales/en/views/system.json` (`models.server.partial`) | The backend reports `partial` when the host collector could not measure some containers (`fork/monitoring/collect_proxmox.py`), and the scopes it lists are current. The panel showed "Host collector is missing or stale" for every status other than `connected`, so a partial snapshot showed real numbers under that warning. Only stale, invalid, not connected or missing data warn now, and a partial snapshot gets its own note. Found by a bug hunt | fork-only |
| C19 | web: error handling | hunk in `web/src/pages/Explore.tsx` (`onError` of the tracked object list, `AxiosError` import); case in `web/e2e/specs/explore.spec.ts` | The list's `onError` read `error.response.data.message` and `error.response.status`, but a network drop or timeout has no `response`, so the handler threw a TypeError, no toast appeared and the rejection went unhandled. It now shows `error.message` when there is no response message and checks the 404 with optional chaining. Found by a bug hunt | candidate |
