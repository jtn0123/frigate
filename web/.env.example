# Copy to web/.env (git-ignored). The dev server proxies /api, /ws, /live,
# /vod, /clips and /exports to this host.
PROXY_HOST=localhost:5000

# Playwright preview port. Each worktree needs its own when two suites run
# at once (see fork/PLAN.md). Defaults to 4173.
# E2E_PORT=4173
